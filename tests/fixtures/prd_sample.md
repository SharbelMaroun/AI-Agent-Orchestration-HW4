# Functional Requirements

- **FR-01** — Clone the checkout service repository from configuration.
- **FR-02** — Run the billing pipeline and export the payments report.

## Non-Functional Requirements

- **NFR-01** — All external calls route through the gatekeeper.
- **NFR-02** — Persist an audit ledger of every session token.
