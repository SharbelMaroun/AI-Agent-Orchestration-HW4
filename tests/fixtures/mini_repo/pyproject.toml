[project]
name = "mini-pkg"
version = "1.00"
description = "Fixture mini repo for ArchLens clone/validation tests"
requires-python = ">=3.10"

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"
