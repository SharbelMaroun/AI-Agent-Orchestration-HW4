# Target-Repository Approval Record

Version: 1.00 | Course: AI Agent Orchestration — HW4 (EX04) | Tasks 2.010, 3.009

**Approver:** Lecturer (Dr. Yoram Segal) | **Date:** 2026-06-14

The lecturer has **approved** the architectural-scale target repository — **httpie** (`https://github.com/httpie/cli`)
— with **psf/requests** as the approved fallback. The request and shortlist are recorded in
`docs/approvals/target_repo_request.md` and `docs/REPO_SELECTION.md`. This satisfies the L07 requirement
that the cloned repository carry the lecturer's approval.

## Update (EX04 debugging subject — 2026-06-15 pivot)

The assignment later pivoted its **debugging** subject to **`andela/buggy-python`** — one of the three
lecturer-suggested buggy corpora in EX04 §2 (so it carries the §2 suggestion by name and needs no
separate sign-off; L07 §11.2 notes the repo choice does not affect the grade). It is now the active
`target_repo` in `config/setup.json` (with all three §2 repos listed under `suggested_repos`), and is the
subject of `deliverables/BUG_REPORT.md` and the `obsidian/` investigation vault. **httpie** is retained
as the **large-scale architectural / token-economics demonstration** (the 2 033-node graph study) and as
the configured fallback. The justification for the buggy-python pick is in `README.md` (§2 block) and
`docs/REPO_SELECTION.md` §7.
