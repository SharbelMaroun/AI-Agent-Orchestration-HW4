# TODO — ArchLens (HW4 / EX04)

Version: 1.00 | Status: Living document | Course: AI Agent Orchestration — HW4 (EX04)

## How to read this file

- **Status legend:** every task carries one of four statuses:
  - `TODO` — not started yet.
  - `IN_PROGRESS` — actively being worked on.
  - `DONE` — completed and verified against its DoD.
  - `BLOCKED` — cannot proceed until a dependency or approval gate is resolved.
- **Checkbox convention:** `- [ ]` means the task is not yet done; `- [x]` means the task is `DONE`.
- **DoD (Definition of Done):** per submission guidelines V3, every task carries an explicit DoD — the concrete, verifiable condition that must hold before the task may be marked `DONE`.
- **Living document rule:** this file is updated continuously during development. Statuses, checkboxes, and tasks are revised as work progresses; it always reflects the current state of the project.

## Overview

| Phase | Title | Tasks |
|-------|-------|-------|
| 1 | Project Setup & Tooling | 45 |
| 2 | Documentation & Approval Gates | 45 |
| 3 | Target Repository Module | 45 |
| 4 | Graphify Pipeline Integration | 50 |
| 5 | Obsidian Vault & Navigation Pages | 50 |
| 6 | Graph Analysis Engine | 55 |
| 7 | Reverse-Engineering Deliverables | 45 |
| 8 | SDK Layer & Core Architecture | 50 |
| 9 | API Gatekeeper & Rate Limiting | 50 |
| 10 | Multi-Agent Orchestration (LangGraph) | 55 |
| 11 | Improvement Loop & Stop Conditions | 50 |
| 12 | Token Measurement & Cost Analysis | 45 |
| 13 | Testing & Quality Gates | 55 |
| 14 | SKILL.md, LLM Wiki & Knowledge Assets | 45 |
| 15 | Research, Visualization & Sensitivity Analysis | 40 |
| 16 | CI/CD, README, Prompt Book & Final Submission | 45 |
| **Total** | — | **770** |

## Phase 1 — Project Setup & Tooling

- [x] 1.001 Initialize git repository on branch main for the HW4 project | **Status:** DONE | **DoD:** `git rev-parse --is-inside-work-tree` exits 0 and prints true inside the project root.
- [x] 1.002 Analyze MATERIALS course documents (L07 section 11, Parts A/B/C, Guidelines V3) and extract Phase 1 machine-checkable standards | **Status:** DONE | **DoD:** each of the six setup standards (uv-only, 150-line cap, Ruff 0, coverage >= 85, config trio, .env policy) maps to at least one task in this section.
- [x] 1.003 Commit the MATERIALS/ directory to version control | **Status:** DONE | **DoD:** `git ls-files MATERIALS/` lists at least one file and `git status --porcelain MATERIALS/` is empty.
- [x] 1.004 Write .gitignore covering .env, credentials.json, *.key, *.pem, .venv/, __pycache__/, .pytest_cache/, .ruff_cache/, and .coverage | **Status:** DONE | **DoD:** `git check-ignore .env credentials.json dummy.key dummy.pem` exits 0 and echoes all four paths.
- [x] 1.005 Initialize the uv project with `uv init --package archlens` using src layout and requires-python >=3.10 | **Status:** DONE | **DoD:** pyproject.toml exists at repo root and `uv run python --version` reports Python 3.10 or newer.
- [x] 1.006 Configure [project] metadata in pyproject.toml (name archlens, version 1.00, description, authors, requires-python >=3.10) | **Status:** DONE | **DoD:** `uv run python -c "import tomllib;p=tomllib.load(open('pyproject.toml','rb'))['project'];assert p['name']=='archlens' and p['version']=='1.00'"` exits 0.
- [x] 1.007 Add runtime dependencies with `uv add langgraph anthropic networkx pydantic` | **Status:** DONE | **DoD:** `uv run python -c "import langgraph, anthropic, networkx, pydantic"` exits 0.
- [x] 1.008 Add dev dependencies with `uv add --dev pytest pytest-cov ruff pre-commit` | **Status:** DONE | **DoD:** `uv run pytest --version` and `uv run ruff --version` both exit 0.
- [x] 1.009 Commit uv.lock as the locked dependency manifest | **Status:** DONE | **DoD:** `git ls-files uv.lock` prints uv.lock and `uv lock --check` exits 0.
- [x] 1.010 Verify reproducible environment installation from the lockfile | **Status:** DONE | **DoD:** `uv sync --locked` exits 0 without modifying uv.lock.
- [x] 1.011 Create the src/archlens subpackage skeleton with sdk/, gatekeeper/, agents/, graphops/, vault/, metrics/, shared/ each containing __init__.py | **Status:** DONE | **DoD:** `uv run python -c "import archlens.sdk, archlens.gatekeeper, archlens.agents, archlens.graphops, archlens.vault, archlens.metrics, archlens.shared"` exits 0.
- [x] 1.012 Write the root src/archlens/__init__.py exporting __version__ = "1.00" and an explicit __all__ | **Status:** DONE | **DoD:** `uv run python -c "import archlens;assert archlens.__version__=='1.00' and archlens.__all__"` exits 0.
- [x] 1.013 Define an explicit __all__ list in every subpackage __init__.py | **Status:** DONE | **DoD:** a python one-liner run via `uv run` confirms all 7 subpackage __init__.py files define __all__ (zero files missing it).
- [x] 1.014 Write failing TDD tests in tests/test_version.py asserting VERSION == "1.00" and get_version() returns it | **Status:** DONE | **DoD:** `uv run pytest tests/test_version.py` exits non-zero (red) before the implementation commit.
- [x] 1.015 Implement src/archlens/shared/version.py with VERSION = "1.00" and get_version() | **Status:** DONE | **DoD:** `uv run pytest tests/test_version.py` exits 0 (green).
- [x] 1.016 Write failing TDD tests in tests/test_constants.py asserting named constants exist (CONFIG_DIR, SETUP_FILE, RATE_LIMITS_FILE, LOGGING_CONFIG_FILE, LINE_CAP = 150) | **Status:** DONE | **DoD:** `uv run pytest tests/test_constants.py` exits non-zero (red) before the implementation commit.
- [x] 1.017 Implement src/archlens/shared/constants.py centralizing all magic values behind named constants | **Status:** DONE | **DoD:** `uv run pytest tests/test_constants.py` exits 0 and `uv run ruff check src/archlens/shared/constants.py` reports 0 violations.
- [x] 1.018 Write config/setup.json with version key "1.00", target repo URL/branch, clone directory, Graphify output paths, and Obsidian vault path | **Status:** DONE | **DoD:** `uv run python -c "import json;assert json.load(open('config/setup.json'))['version']=='1.00'"` exits 0.
- [x] 1.019 Write failing TDD tests in tests/test_setup_config.py covering load success into a typed model, missing-file error, and unknown-key rejection | **Status:** DONE | **DoD:** `uv run pytest tests/test_setup_config.py` exits non-zero (red) before the implementation commit.
- [x] 1.020 Implement the SetupConfig pydantic schema model defining every required key and type of setup.json | **Status:** DONE | **DoD:** the load-success and unknown-key tests in tests/test_setup_config.py pass via `uv run pytest`.
- [x] 1.021 Write failing TDD test asserting load_setup() raises ConfigVersionError when the file version differs from "1.00" | **Status:** DONE | **DoD:** the version-mismatch test fails (red) via `uv run pytest tests/test_setup_config.py -k version` before implementation.
- [x] 1.022 Implement load_setup() in shared config loader with runtime config-version validation against shared/version.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_setup_config.py` exits 0 with all loader and version-validation tests green.
- [x] 1.023 Write config/rate_limits.json per the PRD_api_gatekeeper.md §3.1 nested schema: top-level "version" key, a rate_limits.services.default block with requests_per_minute 30, requests_per_hour 500, concurrent_max 5, retry_after_seconds 30, max_retries 3, and a queue block with max_depth and backpressure_warn_ratio | **Status:** DONE | **DoD:** `uv run python -c` JSON assertion confirms the top-level version key, all five rate_limits.services.default values (requests_per_minute, requests_per_hour, concurrent_max, retry_after_seconds, max_retries), and queue.max_depth plus queue.backpressure_warn_ratio are present and exact.
- [x] 1.024 Write failing TDD tests in tests/test_rate_limits_config.py asserting the nested §3.1 schema (top-level version, the five rate_limits.services.default values including concurrent_max, queue.max_depth, queue.backpressure_warn_ratio) loads into a typed model | **Status:** DONE | **DoD:** `uv run pytest tests/test_rate_limits_config.py` exits non-zero (red) before the implementation commit.
- [x] 1.025 Implement the RateLimitsConfig pydantic model and loader reading config/rate_limits.json (nested PRD_api_gatekeeper.md §3.1 schema with the version key validated at boot) | **Status:** DONE | **DoD:** `uv run pytest tests/test_rate_limits_config.py` exits 0 (green).
- [x] 1.026 Write config/logging_config.json as a dictConfig v1 document with console and file handlers, formatters, and an archlens logger level | **Status:** DONE | **DoD:** `uv run python -c "import json,logging.config;logging.config.dictConfig(json.load(open('config/logging_config.json')))"` exits 0.
- [x] 1.027 Write failing TDD test asserting the logging bootstrap configures the "archlens" logger from config/logging_config.json | **Status:** DONE | **DoD:** `uv run pytest tests/test_logging_setup.py` exits non-zero (red) before the implementation commit.
- [x] 1.028 Implement the shared logging bootstrap module applying dictConfig from config/logging_config.json | **Status:** DONE | **DoD:** `uv run pytest tests/test_logging_setup.py` exits 0 (green).
- [x] 1.029 Write .env-example containing dummy values for every required secret (ANTHROPIC_API_KEY, GITHUB_TOKEN) | **Status:** DONE | **DoD:** `git ls-files .env-example` prints the file and every variable name read via os.environ.get in src/ appears in it with a dummy value.
- [x] 1.030 Verify .env is git-ignored and never tracked while .env-example is tracked | **Status:** DONE | **DoD:** `git check-ignore .env` exits 0 and `git ls-files .env` prints nothing.
- [x] 1.031 Write failing TDD import-contract test asserting ArchLensSDK and Gatekeeper classes are importable from sdk/sdk.py and gatekeeper/gatekeeper.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_skeleton_contract.py` exits non-zero (red) before the stub commits.
- [x] 1.032 Implement src/archlens/sdk/sdk.py stub with the ArchLensSDK class as the single SDK entry point exposing version() | **Status:** DONE | **DoD:** `uv run python -c "from archlens.sdk.sdk import ArchLensSDK;assert ArchLensSDK().version()=='1.00'"` exits 0.
- [x] 1.033 Implement src/archlens/gatekeeper/gatekeeper.py stub with the Gatekeeper class loading RateLimitsConfig as the sole external-API call path | **Status:** DONE | **DoD:** `uv run pytest tests/test_skeleton_contract.py` exits 0 (green).
- [x] 1.034 Write the thin CLI src/main.py (argparse only, zero business logic) delegating every action to ArchLensSDK | **Status:** DONE | **DoD:** `uv run python src/main.py --version` prints 1.00 and exits 0.
- [x] 1.035 Write tests/conftest.py with shared fixtures (tmp_path config copies, environment stubs) | **Status:** DONE | **DoD:** `uv run pytest --collect-only -q` exits 0 with all fixtures resolvable.
- [x] 1.036 Configure [tool.ruff] in pyproject.toml with line-length 100, target-version py310, select E,F,W,I,N,UP,B,C4,SIM, ignore E501 | **Status:** DONE | **DoD:** `uv run ruff check .` exits 0 with 0 violations under exactly that rule set.
- [x] 1.037 Configure [tool.pytest.ini_options] and [tool.coverage] in pyproject.toml with branch coverage, fail_under = 85, and omit entries for src/main.py and tests | **Status:** DONE | **DoD:** pyproject.toml contains fail_under = 85 with branch = true and the omit list includes src/main.py.
- [x] 1.038 Run the full test suite with coverage gating to green | **Status:** DONE | **DoD:** `uv run pytest --cov=archlens --cov-branch` exits 0 with TOTAL coverage >= 85%.
- [x] 1.039 Write failing TDD tests in tests/test_line_cap.py for the 150-line checker (blank and comment lines excluded, 150 effective lines pass, 151 fail) | **Status:** DONE | **DoD:** `uv run pytest tests/test_line_cap.py` exits non-zero (red) before the implementation commit.
- [x] 1.040 Implement scripts/check_line_cap.py enforcing the 150 effective-line cap across src/ and tests/ | **Status:** DONE | **DoD:** `uv run pytest tests/test_line_cap.py` exits 0 and `uv run python scripts/check_line_cap.py` exits 0 on the current tree.
- [x] 1.041 Configure .pre-commit-config.yaml with a ruff hook and the line-cap checker hook, both executed through uv | **Status:** DONE | **DoD:** `uv run pre-commit run --all-files` exits 0.
- [x] 1.042 Install the pre-commit hook into the local git repository | **Status:** DONE | **DoD:** after `uv run pre-commit install`, the file .git/hooks/pre-commit exists and a commit containing a deliberate ruff violation is rejected (hook exits non-zero).
- [x] 1.043 Verify zero forbidden tooling references in the repository (pip, virtualenv, venv, "python -m", requirements.txt) outside MATERIALS/ and .venv/ | **Status:** DONE | **DoD:** a recursive text search over tracked project files (excluding MATERIALS/) returns 0 matches for the forbidden terms.
- [x] 1.044 Document the uv-only quickstart in README.md (uv sync, uv run pytest, uv run ruff check ., uv run python src/main.py) | **Status:** DONE | **DoD:** README.md contains all four uv commands and zero occurrences of pip, venv, or requirements.txt.
- [x] 1.045 Commit the Phase 1 baseline (skeleton, config trio, .env-example, hooks, lockfile) as a single reviewed commit | **Status:** DONE | **DoD:** `git status --porcelain` is empty and the latest commit tracks pyproject.toml, uv.lock, config/setup.json, config/rate_limits.json, config/logging_config.json, .env-example, and .pre-commit-config.yaml.

## Phase 2 — Documentation & Approval Gates

- [x] 2.001 Analyze all course source documents in MATERIALS/ (L07, Part A, Part B, Part C, Guidelines V3) and extract the graded documentation deliverables list | **Status:** DONE | **DoD:** Every mandated doc (PRD, PLAN, TODO, PROMPT_BOOK, 5 specialized PRDs, ADRs) appears as a task in this section with a course-source origin
- [x] 2.002 Create the docs/ skeleton (docs/, docs/adr/, docs/diagrams/, docs/approvals/) | **Status:** DONE | **DoD:** Directories docs/adr/, docs/diagrams/, and docs/approvals/ exist with .gitkeep files tracked by git
- [x] 2.003 Write the shared doc header template docs/_templates/doc_header.md with the mandatory header (Version: 1.00, Status: Draft — awaiting lecturer approval, Course: AI Agent Orchestration — HW4 (EX04)) | **Status:** DONE | **DoD:** Template file exists containing the exact mandatory header text
- [x] 2.004 Document the doc versioning convention in docs/VERSIONING.md (two-decimal versions starting at 1.00, bump triggers on lecturer feedback, per-doc change-log table) | **Status:** DONE | **DoD:** docs/VERSIONING.md exists and defines start version 1.00, bump rules, and the change-log table format
- [x] 2.005 Write PRD.md draft covering goals, scope, the 7-agent roster plus supervisor, and all EX04 deliverables | **Status:** DONE | **DoD:** docs/PRD.md exists with the mandatory header and contains at least 15 FR-xx and 10 NFR-xx requirement IDs
- [x] 2.006 Document the FR catalogue in PRD.md with a traceability table mapping all 5 L07 section-11 EX04 core tasks to FR IDs | **Status:** DONE | **DoD:** Traceability table maps each of the 5 EX04 core tasks to at least one FR-xx with zero unmapped tasks
- [x] 2.007 Document the NFR catalogue in PRD.md covering the 5 improvement-loop stop conditions, 5-iteration hard cap, coverage >= 85%, Ruff 0 violations, 150-line file limit, and uv-only tooling | **Status:** DONE | **DoD:** Each listed constraint appears as its own NFR-xx with a measurable threshold value
- [x] 2.008 Document the target-repository shortlist (at least 3 BugsInPy candidates plus 1 simpler fallback) with selection criteria in a PRD.md appendix | **Status:** DONE | **DoD:** Appendix lists >= 3 candidates, each with repo URL, uv-compatibility note, and test-suite status
- [ ] 2.009 Write the target-repo approval request to the lecturer referencing the PRD shortlist and store the sent copy | **Status:** IN_PROGRESS | **DoD:** docs/approvals/target_repo_request.md exists containing the shortlist and a sent-date stamp
- [ ] 2.010 Obtain lecturer approval of the chosen target repository | **Status:** BLOCKED | **DoD:** Written lecturer approval (repo name plus date) recorded in docs/approvals/target_repo_approval.md
- [x] 2.011 Verify PRD.md against a self-review checklist (every requirement has a unique FR-xx/NFR-xx ID, is testable, and no tooling other than uv is referenced for use) | **Status:** DONE | **DoD:** Review checklist appended to the PRD.md change log with 0 open findings
- [ ] 2.012 Obtain lecturer approval of PRD.md (workflow gate: PRD approved before other docs are finalized) | **Status:** BLOCKED | **DoD:** PRD.md Status header changed from Draft to Approved and confirmation stored in docs/approvals/prd_approval.md
- [x] 2.013 Write PLAN.md draft covering architecture overview, the src/archlens module layout (sdk, gatekeeper, agents, graphops, vault, metrics, shared), and milestone plan | **Status:** DONE | **DoD:** docs/PLAN.md exists with the mandatory header and dedicated sections for C4, UML, deployment, and the ADR index
- [x] 2.014 Create the C4 Level 1 system-context mermaid diagram in PLAN.md (user, ArchLens, GitHub target repo, LLM API via gatekeeper, Obsidian vault) | **Status:** DONE | **DoD:** Fenced mermaid block present in PLAN.md showing ArchLens with >= 4 surrounding external actors/systems
- [x] 2.015 Create the C4 Level 2 container mermaid diagram in PLAN.md (thin CLI, SDK, gatekeeper, LangGraph runtime, config files, vault output) | **Status:** DONE | **DoD:** Fenced mermaid block depicts the CLI as a zero-business-logic shell delegating only to the SDK
- [x] 2.016 Create the C4 Level 3 component mermaid diagram in PLAN.md showing the supervisor and all 7 agents (Repo, Graph, Analyst, BugHunter, Refactor, QA, Metrics) with their edges | **Status:** DONE | **DoD:** Fenced mermaid block names the supervisor node and all 7 agent nodes
- [x] 2.017 Create the UML class mermaid diagram (classDiagram) in PLAN.md for the SDK, gatekeeper, shared agent base class, and the 7 concrete agents | **Status:** DONE | **DoD:** classDiagram block contains >= 10 classes with all 7 agents inheriting from a shared base
- [x] 2.018 Create the sequence mermaid diagram in PLAN.md for one improvement-loop iteration (RefactorAgent fix, Graphify re-run, metrics diff, QAAgent tests) | **Status:** DONE | **DoD:** sequenceDiagram block shows a QAAgent pytest run occurring after every RefactorAgent change
- [x] 2.019 Create the state mermaid diagram in PLAN.md for the LangGraph StateGraph including stop-condition exits and the 5-iteration hard cap | **Status:** DONE | **DoD:** stateDiagram block contains a terminal state reachable from both the stop-conditions-met and iteration-cap transitions
- [x] 2.020 Create the deployment mermaid diagram in PLAN.md (developer machine with uv-managed environment, cloned target repo, Obsidian vault directory, external LLM API) | **Status:** DONE | **DoD:** Fenced mermaid block depicts the uv-managed runtime and places the external LLM API behind the gatekeeper boundary
- [x] 2.021 Verify all 7 PLAN.md mermaid diagrams compile without syntax errors | **Status:** DONE | **DoD:** mermaid-cli compilation of all 7 extracted fenced blocks exits with code 0
- [x] 2.022 Write the ADR template docs/adr/ADR-000-template.md (Status, Context, Decision, Alternatives, Consequences sections) and ADR-001 documenting the choice of LangGraph over CrewAI for multi-agent orchestration | **Status:** DONE | **DoD:** Template file exists with all 5 section headings and docs/adr/ADR-001-langgraph-over-crewai.md exists with CrewAI listed as a rejected alternative and an explicit Status field
- [x] 2.023 Write ADR-002 documenting the uv-only toolchain decision (pip, virtualenv, venv, and "python -m" forbidden; pyproject.toml plus committed uv.lock as single source of truth) | **Status:** DONE | **DoD:** docs/adr/ADR-002-uv-only-toolchain.md exists and cites Guidelines V3 as the decision driver
- [x] 2.024 Write ADR-003 documenting the single SDK entry point (all business logic behind src/archlens/sdk/sdk.py with a thin src/main.py CLI) | **Status:** DONE | **DoD:** docs/adr/ADR-003-sdk-entry-point.md exists with the zero-logic CLI constraint listed under Consequences
- [x] 2.025 Write ADR-004 documenting the gatekeeper FIFO never-reject design: sole egress for external API calls with rate limits (requests_per_minute 30, requests_per_hour 500, concurrent_max 5, retry_after_seconds 30, max_retries 3) and an overflow queue that queues, never rejects or crashes | **Status:** DONE | **DoD:** docs/adr/ADR-004-gatekeeper-fifo-never-reject.md exists listing all 5 numeric limits matching config/rate_limits.json and naming the queue.max_depth config key
- [x] 2.026 Write ADR-005 documenting the improvement-loop stop conditions per Part C p21 (all 5 stop conditions plus the MAX_LOOP_ITERATIONS=5 hard cap from shared/constants.py) | **Status:** DONE | **DoD:** docs/adr/ADR-005-stop-conditions.md exists citing Part C p21 and stating all 5 stop conditions and the 5-iteration cap
- [x] 2.027 Write ADR-006 documenting the token-measurement baseline methodology (naive full-context baseline vs Graphify-assisted run, >= 70% savings target, amortization fallback explanation per Part A) | **Status:** DONE | **DoD:** docs/adr/ADR-006-token-measurement-baseline.md exists defining both measurement runs and the per-model cost-table format
- [x] 2.028 Write ADR-007 documenting evidence-ladder enforcement (OBSERVED, INFERRED, EXTRACTED, VALIDATED levels; confidence thresholds: edge triage 0.55-0.95, duplicate similarity >= 0.91) | **Status:** DONE | **DoD:** docs/adr/ADR-007-evidence-ladder-enforcement.md exists with every threshold value stated numerically
- [x] 2.029 Write ADR-008 documenting the hot.md definition (hot-node qualification criteria, ranking rules, and hot.md's role as the vault entry point) | **Status:** DONE | **DoD:** docs/adr/ADR-008-hot-md-definition.md exists with measurable hot-node criteria and an explicit Status field
- [x] 2.030 Write ADR-009 documenting the SQLite checkpointer plus approval interrupts (LangGraph SqliteSaver persistence and human-approval interrupt points in the graph) | **Status:** DONE | **DoD:** docs/adr/ADR-009-sqlite-checkpointer-interrupts.md exists naming the checkpointer backend and every approval interrupt point
- [x] 2.031 Write ADR-010 documenting the fix-priority taxonomy P1-P5 and the ADR index docs/adr/index.md linking every ADR with its status | **Status:** DONE | **DoD:** docs/adr/ADR-010-fix-priority-taxonomy.md exists defining all five P1-P5 priority levels and the index lists >= 10 ADRs, each with title, status, and a working relative link
- [x] 2.032 Draft docs/PRD_api_gatekeeper.md, the specialized PRD for the rate-limited API gatekeeper mechanism | **Status:** DONE | **DoD:** docs/PRD_api_gatekeeper.md exists with FR/NFR IDs covering all rate limits, the retry policy, and FIFO never-reject queue behavior
- [x] 2.033 Draft docs/PRD_agent_orchestration.md, the specialized PRD for the LangGraph multi-agent orchestration mechanism (AgentState contract, supervisor pattern, per-key reducers) | **Status:** DONE | **DoD:** docs/PRD_agent_orchestration.md exists with FR/NFR IDs covering the AgentState schema and the §3.2 reducer rules (dict fields last-write-wins; findings/approvals append-only)
- [x] 2.034 Draft docs/PRD_improvement_loop.md, the specialized PRD for the refactor, re-graph, diff loop with stop conditions | **Status:** DONE | **DoD:** docs/PRD_improvement_loop.md exists listing all 5 stop conditions and the 5-iteration cap as FR/NFR IDs
- [x] 2.035 Draft docs/PRD_graph_pipeline.md, the specialized PRD for the Graphify pipeline (detect, extract, build, cluster, export) and Obsidian vault build | **Status:** DONE | **DoD:** docs/PRD_graph_pipeline.md exists covering graph.json, graph.html, REPORT.md, hot.md, index.md, wiki/, and log.md outputs
- [x] 2.036 Draft docs/PRD_token_metrics.md, the specialized PRD for token before/after accounting and per-model cost tables | **Status:** DONE | **DoD:** docs/PRD_token_metrics.md exists specifying the >= 70% savings target and the written-explanation fallback requirement
- [x] 2.037 Write TODO.md draft aggregating all phase sections with per-task status and DoD | **Status:** DONE | **DoD:** docs/TODO.md exists and every task line carries a Status field and a measurable DoD
- [x] 2.038 Write PROMPT_BOOK.md draft with the prompt-engineering log template (goal, prompt, model, outcome, iteration) and the first logged entries | **Status:** DONE | **DoD:** docs/PROMPT_BOOK.md exists with the entry template and >= 3 logged prompts from this documentation phase
- [x] 2.039 Write the README.md outline plan (section skeleton: overview, uv-based setup, usage, architecture, agents, results, token economics) | **Status:** DONE | **DoD:** docs/README_OUTLINE.md exists listing >= 7 planned sections with a one-line purpose for each
- [x] 2.040 Write the project glossary docs/GLOSSARY.md (Graphify, hot.md, god node, hub vs bottleneck, bridge, evidence ladder, SPOF, FIFO overflow queue, and related terms) | **Status:** DONE | **DoD:** Glossary defines >= 25 terms, each with a one-sentence definition and a course-source reference
- [x] 2.041 Verify cross-document requirement traceability (every PRD FR-xx/NFR-xx appears in PLAN.md and/or TODO.md) | **Status:** DONE | **DoD:** Traceability check table committed in PLAN.md shows 0 orphan requirement IDs
- [x] 2.042 Verify uv-only language compliance across docs/ | **Status:** DONE | **DoD:** Ripgrep search for "pip install", "venv", and "requirements.txt" across docs/ returns 0 matches outside explicit prohibition statements
- [x] 2.043 Verify the mandatory header block in every deliverable doc (PRD, PLAN, TODO, PROMPT_BOOK, 5 specialized PRDs, GLOSSARY, all ADRs) | **Status:** DONE | **DoD:** Ripgrep confirms the "Version: 1.00" header line is present in every deliverable doc with zero files missing it
- [ ] 2.044 Incorporate lecturer feedback into all docs, bump versions per docs/VERSIONING.md, and update each change log | **Status:** TODO | **DoD:** Every feedback item tracked in docs/approvals/feedback_log.md is marked resolved with the doc version that addressed it
- [ ] 2.045 Obtain lecturer approval of ALL documentation (workflow gate: development cannot start before this) | **Status:** BLOCKED | **DoD:** Every deliverable doc's Status header reads Approved and the confirmation is stored in docs/approvals/all_docs_approval.md

## Phase 3 — Target Repository Module

- [x] 3.001 Analyze MATERIALS/ course sources (L07 section 11, Parts A/B/C, Guidelines V3) for target-repository requirements | **Status:** DONE | **DoD:** MATERIALS/ directory exists at repo root and contains the course summary documents cited by Phase 3 tasks
- [x] 3.002 Review and extend the target-repository FR group in docs/PRD.md (and its PLAN.md coverage) with FR-xx/NFR-xx requirement IDs for the target-repository mechanism | **Status:** DONE | **DoD:** The docs/PRD.md target-repository FR group enumerates at least 8 FR-xx items covering selection, clone, validation, and isolation
- [x] 3.003 Write docs/REPO_SELECTION.md defining measurable repo selection criteria (Python 3.10+ compatible, LOC bounds, pytest suite present, OSI license, Graphify-parseable) | **Status:** DONE | **DoD:** Document lists at least 6 criteria, each paired with an objective pass/fail check command
- [x] 3.004 Compile a candidate shortlist of at least 3 BugsInPy repos plus at least 2 simpler fallback repos into docs/REPO_SELECTION.md | **Status:** DONE | **DoD:** Shortlist table contains at least 5 rows with columns repo, LOC, test count, license, and uv compatibility
- [x] 3.005 Evaluate every shortlisted BugsInPy candidate for mandatory uv-managed environment feasibility (uv sync from pyproject.toml) | **Status:** DONE | **DoD:** Each shortlist row is marked uv-OK or uv-FAIL with the exact command output captured as evidence
- [x] 3.006 Verify the top BugsInPy candidate clones and runs its test suite via "uv run pytest" in a uv-managed environment | **Status:** DONE | **DoD:** "uv run pytest" completes in the candidate checkout with pass counts and any documented known-failing bug tests recorded in docs/REPO_SELECTION.md
- [x] 3.007 Document the fallback simpler-repo path as a config-only switch (no code changes required) in docs/REPO_SELECTION.md | **Status:** DONE | **DoD:** Fallback section names the exact config/setup.json keys to change and includes a dry-run log demonstrating the switch
- [x] 3.008 Write the final primary-plus-fallback repo recommendation with per-criterion scores in docs/REPO_SELECTION.md | **Status:** DONE | **DoD:** Recommendation section names exactly one primary and one fallback repo with a completed criteria score table
- [ ] 3.009 Obtain lecturer approval for the selected target repo (or the simpler fallback repo) | **Status:** BLOCKED | **DoD:** Written lecturer approval with date is recorded in the approval section of docs/REPO_SELECTION.md
- [x] 3.010 Configure the target_repo block in config/setup.json (url, branch, pinned commit, workdir root, clone depth, timeout_s, max_size_mb) | **Status:** DONE | **DoD:** config/setup.json parses as valid JSON and contains all 7 listed keys under target_repo
- [x] 3.011 Configure the fallback_repo block in config/setup.json mirroring the target_repo schema | **Status:** DONE | **DoD:** fallback_repo block contains the identical key set as target_repo and passes the config schema test
- [x] 3.012 Write failing unit tests for the target-repo config accessor (missing keys, bad URL scheme, default values) before implementation | **Status:** DONE | **DoD:** "uv run pytest tests/test_repo_config.py" fails red with at least 5 test cases collected
- [x] 3.013 Implement the target-repo config accessor in the SDK reading only via cfg.get with no hardcoded values | **Status:** DONE | **DoD:** tests/test_repo_config.py passes green and "uv run ruff check" reports 0 violations on the new module
- [x] 3.014 Add repo-module constants (allowed URL schemes, default clone depth, size-bound defaults) to src/archlens/shared/constants.py | **Status:** DONE | **DoD:** Grep for the literal values inside src/archlens returns hits only in constants.py
- [x] 3.015 Write failing unit tests for the sandbox workdir manager (per-run isolated directory creation, path containment, cleanup) | **Status:** DONE | **DoD:** "uv run pytest tests/test_sandbox.py" fails red with creation, containment, and cleanup cases collected
- [x] 3.016 Implement the sandbox workdir manager creating an isolated per-run directory under the configured workdir root | **Status:** DONE | **DoD:** tests/test_sandbox.py creation and containment tests pass green
- [x] 3.017 Implement idempotent sandbox cleanup (safe on re-run, removes stale workdirs, never deletes paths outside the sandbox root) | **Status:** DONE | **DoD:** Cleanup tests pass green including a double-invocation case raising no exception
- [x] 3.018 Verify clone destinations are git-ignored and located outside the project source tree | **Status:** DONE | **DoD:** .gitignore covers the workdir root pattern and a unit test asserts the resolved clone path is not under src/
- [x] 3.019 Build a fixture mini-repo under tests/fixtures/mini_repo (small Python package with pyproject.toml, tests/ directory, LICENSE, 2+ modules) | **Status:** DONE | **DoD:** Fixture directory contains pyproject.toml, LICENSE, a tests/ subdirectory, and at least 2 .py modules
- [x] 3.020 Add a conftest.py fixture factory that materializes mini_repo as a real git repository inside tmp_path | **Status:** DONE | **DoD:** A smoke test asserts the fixture-built repo has at least 1 git commit and the fixture is consumed by clone tests
- [x] 3.021 Add negative test fixtures (non-Python repo, oversize repo, repo without tests, repo without LICENSE) | **Status:** DONE | **DoD:** Four distinct negative fixture variants exist and each is referenced by at least one validation test
- [x] 3.022 Write failing unit tests with mocked git subprocess for the gatekeeper git-clone wrapper (success, non-zero exit, timeout) | **Status:** DONE | **DoD:** "uv run pytest tests/test_gatekeeper_git.py" fails red with mock assertions proving no real network or git binary is invoked
- [x] 3.023 Implement the gatekeeper git_clone wrapper so ALL git network operations route through src/archlens/gatekeeper/gatekeeper.py | **Status:** DONE | **DoD:** tests/test_gatekeeper_git.py passes green using mocked subprocess only
- [x] 3.024 Implement shallow-clone depth and branch/commit pinning in the gatekeeper clone wrapper driven by setup.json values | **Status:** DONE | **DoD:** Unit test asserts the mocked git command line contains the configured depth, branch, and pinned commit arguments
- [x] 3.025 Write failing unit tests for the SDK clone_target_repo entry point with the gatekeeper fully mocked | **Status:** DONE | **DoD:** New SDK tests fail red and assert clone_target_repo delegates to the gatekeeper mock exactly once per call
- [x] 3.026 Implement clone_target_repo in src/archlens/sdk/sdk.py as the single public clone API with no business logic outside the SDK | **Status:** DONE | **DoD:** SDK clone tests pass green and the function returns the sandboxed checkout path
- [x] 3.027 Verify no module outside the gatekeeper invokes git or network calls directly | **Status:** DONE | **DoD:** A guard test scans src/archlens excluding gatekeeper/ for subprocess git usage and asserts 0 matches
- [x] 3.028 Write failing unit tests for the is-Python validation check using positive and non-Python fixtures | **Status:** DONE | **DoD:** is-Python tests fail red covering pass, fail, and empty-repo edge cases
- [x] 3.029 Implement the is-Python validation check (.py file share above configured threshold plus Python project config presence) | **Status:** DONE | **DoD:** is-Python tests pass green for mini_repo and correctly reject the non-Python fixture
- [x] 3.030 Write failing unit tests for the size-bounds validation check (below minimum, within bounds, above maximum from config) | **Status:** DONE | **DoD:** Size-bounds tests fail red with all three boundary cases collected
- [x] 3.031 Implement the size-bounds validation check using min/max file-count and MB limits read from setup.json | **Status:** DONE | **DoD:** Size-bounds tests pass green including rejection of the oversize fixture
- [x] 3.032 Write failing unit tests for the has-tests validation check (tests/ directory or pytest configuration detection) | **Status:** DONE | **DoD:** Has-tests tests fail red covering both detection paths and the no-tests fixture
- [x] 3.033 Implement the has-tests validation check detecting a test suite in the cloned target | **Status:** DONE | **DoD:** Has-tests tests pass green for mini_repo and reject the no-tests fixture
- [x] 3.034 Write failing unit tests for the license validation check (LICENSE file presence and recognized SPDX identifier) | **Status:** DONE | **DoD:** License tests fail red covering recognized, unrecognized, and missing-license cases
- [x] 3.035 Implement the license validation check with an explicit failure reason string | **Status:** DONE | **DoD:** License tests pass green and the missing-license fixture is rejected with a non-empty reason
- [x] 3.036 Implement the aggregated validate_repo SDK function returning a ValidationResult with per-check status and failure reasons | **Status:** DONE | **DoD:** Unit test asserts ValidationResult lists all 4 checks with individual pass/fail flags and human-readable reasons
- [x] 3.037 Test the aggregated validation against every negative fixture so each fails exactly its targeted check | **Status:** DONE | **DoD:** Parametrized test passes showing one and only one failing check per negative fixture
- [x] 3.038 Write failing unit tests for the RepoAgent LangGraph node (state in: config; state out: repo_path and validation report) with the SDK mocked | **Status:** DONE | **DoD:** RepoAgent tests fail red and assert the expected state mutation keys without performing any real clone
- [x] 3.039 Implement the RepoAgent node in src/archlens/agents/ calling SDK clone and validate and writing results into the graph state | **Status:** DONE | **DoD:** RepoAgent tests pass green and the node registers in the StateGraph without import errors
- [x] 3.040 Implement RepoAgent fallback behavior switching to fallback_repo config after primary validation failure (single fallback attempt) | **Status:** DONE | **DoD:** Unit test with a failing primary mock shows exactly one fallback clone attempt and the state flag fallback_used set to true
- [x] 3.041 Write failing unit tests for the clone error taxonomy (network failure, auth failure, timeout, disk full) using mocked git errors | **Status:** DONE | **DoD:** Error-taxonomy tests fail red with one case per failure class mapped to a distinct expected exception type
- [x] 3.042 Implement typed repo-module exceptions and structured error handling (no bare except, every failure logged per logging_config.json) | **Status:** DONE | **DoD:** Error-taxonomy tests pass green and "uv run ruff check" reports 0 violations including no bare-except findings
- [x] 3.043 Implement the clone retry policy per config/rate_limits.json (max_retries 3, retry_after 30s backoff, FIFO overflow queue, never reject or crash) | **Status:** DONE | **DoD:** A unit test asserts all retry parameters are read from rate_limits.json with zero hardcoded retry values in source
- [x] 3.044 Test the retry policy against mocked transient git failures (success on attempt 3, exhaustion after max_retries) | **Status:** DONE | **DoD:** Tests pass green asserting exactly 3 attempts before success and a typed exhaustion error after max_retries
- [x] 3.045 Verify Phase 3 quality gates on the repo module (coverage, lint, file-length cap) | **Status:** DONE | **DoD:** "uv run pytest --cov" reports at least 85% for repo-module files, "uv run ruff check ." exits 0, and every Phase 3 file has at most 150 non-blank non-comment lines

## Phase 4 — Graphify Pipeline Integration

- [x] 4.001 Mechanism-team review of docs/PRD_graph_pipeline.md for the Graphify integration mechanism (FR/NFR IDs, mermaid pipeline diagram, V1.00 header) | **Status:** DONE | **DoD:** Review findings recorded in the PRD_graph_pipeline.md change log with 0 open findings; V1.00 header, FR-xx/NFR-xx requirement IDs, and a mermaid pipeline diagram confirmed present
- [ ] 4.002 Submit PRD_graph_pipeline.md to the lecturer for approval before Phase 4 implementation starts | **Status:** BLOCKED | **DoD:** Doc header Status field changed from "Draft — awaiting lecturer approval" to "Approved" with date
- [x] 4.003 Configure a "graphify" block in config/setup.json (binary path, stage list, output root, timeout_s) | **Status:** DONE | **DoD:** All four keys readable via cfg.get in a passing unit test with no hardcoded fallbacks in src
- [x] 4.004 Add Graphify stage names and artifact filenames (graph.json, graph.html, REPORT.md) to shared/constants.py | **Status:** DONE | **DoD:** Constants imported by graphops modules and `uv run ruff check src` reports 0 violations
- [x] 4.005 Write failing unit tests for a GraphifyConfig pydantic model covering required keys and invalid types | **Status:** DONE | **DoD:** `uv run pytest tests/test_graphify_config.py` fails red with the model not yet implemented
- [x] 4.006 Implement GraphifyConfig pydantic model in src/archlens/graphops/config.py loading values from setup.json | **Status:** DONE | **DoD:** All tests in tests/test_graphify_config.py pass and the file is ≤150 code lines
- [x] 4.007 Write failing tests for analysis-depth modes asserting structural-only skips semantic stages and semantic enables them | **Status:** DONE | **DoD:** New tests fail red against current GraphifyConfig with mode field absent
- [x] 4.008 Implement analysis_depth field (structural|semantic) on GraphifyConfig driving stage selection | **Status:** DONE | **DoD:** Analysis-depth tests pass and mode value comes only from setup.json via cfg.get
- [x] 4.009 Write failing tests for per-run token budget enforcement (budget read from config, exceeded budget aborts semantic pass) | **Status:** DONE | **DoD:** Tests fail red asserting a TokenBudgetExceeded error not yet defined
- [x] 4.010 Implement token budget guard in graphops that raises TokenBudgetExceeded before launching semantic analysis over budget | **Status:** DONE | **DoD:** Budget tests pass with budget value sourced from setup.json, never hardcoded
- [x] 4.011 Write failing tests for GraphifyCLI command construction (uv run invocation, per-stage flags, target repo path quoting) | **Status:** DONE | **DoD:** Tests fail red asserting exact argv lists the wrapper must build
- [x] 4.012 Implement GraphifyCLI wrapper in src/archlens/graphops/cli_wrapper.py building argv and capturing stdout/stderr/exit code | **Status:** DONE | **DoD:** Command-construction tests pass and no subprocess call exists outside the wrapper's execution path
- [x] 4.013 Route GraphifyCLI subprocess execution through gatekeeper/gatekeeper.py as the single external-call chokepoint | **Status:** DONE | **DoD:** `grep -r subprocess src/archlens --include=*.py` matches only gatekeeper-owned modules
- [x] 4.014 Write failing tests for a PipelineStage enum enforcing order detect→extract→build→cluster→export | **Status:** DONE | **DoD:** Test asserting the exact five-stage ordering fails red before implementation
- [x] 4.015 Implement PipelineStage enum and stage ordering helper in src/archlens/graphops/stages.py | **Status:** DONE | **DoD:** Stage-order tests pass and ruff reports 0 violations on stages.py
- [x] 4.016 Write failing tests for the stage orchestrator covering full happy path and halt-on-first-failure behavior | **Status:** DONE | **DoD:** Tests fail red asserting StageResult records and no later stage runs after a failure
- [x] 4.017 Implement stage orchestrator in src/archlens/graphops/orchestrator.py emitting a StageResult per stage | **Status:** DONE | **DoD:** Orchestrator tests pass with a 5-element StageResult list on the happy-path fixture run
- [x] 4.018 Write failing tests for the run-scoped output directory layout builder | **Status:** DONE | **DoD:** Test asserting artifacts/runs/<run_id>/{graph.json,graph.html,REPORT.md} path scheme fails red
- [x] 4.019 Implement output layout module src/archlens/graphops/layout.py creating per-run artifact directories | **Status:** DONE | **DoD:** Layout tests pass and tmp_path-based test shows the exact directory tree created
- [x] 4.020 Write failing tests for run ID generation guaranteeing uniqueness and lexicographic sortability across rapid calls | **Status:** DONE | **DoD:** Test generating 100 consecutive run IDs with zero collisions fails red pre-implementation
- [x] 4.021 Implement run ID generator and manifest.json persistence recording stage results and artifact paths per run | **Status:** DONE | **DoD:** Run ID tests pass and manifest.json validates against its pydantic schema in a unit test
- [x] 4.022 Create fixture graph.json files (minimal, full-featured with hyperedges/rationale nodes, malformed) under tests/fixtures/graphify/ | **Status:** DONE | **DoD:** Three fixture files exist and the full-featured one contains nodes, edges, communities, hyperedges, and rationale nodes
- [x] 4.023 Write failing tests for the Node pydantic model (id, kind, source_file, metrics fields) | **Status:** DONE | **DoD:** Node validation tests fail red with import error on the unbuilt model
- [x] 4.024 Implement Node model in src/archlens/graphops/models/node.py | **Status:** DONE | **DoD:** Node tests pass and the minimal fixture's nodes all parse without error
- [x] 4.025 Write failing tests for the Edge model requiring relation, type, confidence in [0.55, 0.95], and source_file citation | **Status:** DONE | **DoD:** Tests fail red including a case rejecting confidence 0.50 and one rejecting a missing source_file
- [x] 4.026 Implement Edge model with confidence range validator and EXTRACTED/INFERRED/AMBIGUOUS triage type enum | **Status:** DONE | **DoD:** All Edge tests pass including boundary values 0.55 and 0.95 accepted
- [x] 4.027 Write failing tests for the Community model validating member node ID references | **Status:** DONE | **DoD:** Test rejecting a community referencing an unknown node ID fails red
- [x] 4.028 Implement Community model with membership cross-reference validation against the node set | **Status:** DONE | **DoD:** Community tests pass against the full-featured fixture's community list
- [x] 4.029 Write failing tests for Hyperedge and RationaleNode models (multi-node membership, rationale text linkage) | **Status:** DONE | **DoD:** Tests fail red covering a hyperedge with 3+ members and a rationale node linked to an edge ID
- [x] 4.030 Implement Hyperedge and RationaleNode models in src/archlens/graphops/models/ | **Status:** DONE | **DoD:** Both model test files pass and each model file is ≤150 code lines
- [x] 4.031 Write failing tests for the Graph aggregate parser loading the full-featured fixture end to end | **Status:** DONE | **DoD:** Test asserting exact node/edge/community/hyperedge counts from the fixture fails red
- [x] 4.032 Write failing tests for parser rejection of the malformed fixture with an actionable error message | **Status:** DONE | **DoD:** Test asserting GraphParseError naming the offending JSON path fails red
- [x] 4.033 Implement graph.json parser in src/archlens/graphops/parser.py returning a validated Graph aggregate | **Status:** DONE | **DoD:** Both parser test files pass and malformed input raises GraphParseError, never a bare KeyError
- [x] 4.034 Write failing tests for the graph.html artifact handler (copy into run dir, SHA-256 checksum, manifest registration) | **Status:** DONE | **DoD:** Tests fail red asserting checksum and manifest entry for a fixture graph.html
- [x] 4.035 Implement graph.html artifact handler registering the file and checksum in the run manifest | **Status:** DONE | **DoD:** Artifact-handler tests pass and manifest.json contains the graph.html checksum entry
- [x] 4.036 Write failing tests for REPORT.md ingestion extracting section headings, metrics tables, and findings into records | **Status:** DONE | **DoD:** Tests against a fixture REPORT.md asserting parsed section count and one metric value fail red
- [x] 4.037 Implement REPORT.md ingestion module producing structured ReportSummary records for downstream agents | **Status:** DONE | **DoD:** Report ingestion tests pass and ReportSummary serializes to JSON round-trip in a test
- [x] 4.038 Write failing tests for re-run support (second run gets a new run_id, first run's artifacts remain unmodified) | **Status:** DONE | **DoD:** Test comparing pre/post checksums of run-1 artifacts after run-2 fails red
- [x] 4.039 Implement re-run support linking successive runs in a manifest history list for the improvement loop | **Status:** DONE | **DoD:** Re-run tests pass and manifest history lists both run IDs in execution order
- [x] 4.040 Write failing tests for subtree cache keys (content-hash of directory tree, hit on unchanged, miss on any file edit) | **Status:** DONE | **DoD:** Tests fail red covering one cache-hit and one cache-miss scenario on a tmp_path tree
- [x] 4.041 Implement unchanged-subtree caching that skips the extract stage for cache-hit directories and logs the skip | **Status:** DONE | **DoD:** Cache tests pass and orchestrator test proves extract is not invoked for an unchanged subtree
- [x] 4.042 Write failing tests for the GraphDiff data model (added/removed nodes and edges, inter-community edge delta, new isolated components flag) | **Status:** DONE | **DoD:** Tests fail red asserting every Part C stop-condition field exists on the model
- [x] 4.043 Implement GraphDiff data model in src/archlens/graphops/diff.py | **Status:** DONE | **DoD:** GraphDiff model tests pass and the model exposes all stop-condition fields consumed by the loop
- [x] 4.044 Write failing tests for diff computation between two fixture graphs with known expected deltas | **Status:** DONE | **DoD:** Tests asserting exact added/removed counts and inter-community edge delta fail red
- [x] 4.045 Implement graph diff computation including bottleneck dependency-loss detection and isolated-component check | **Status:** DONE | **DoD:** Diff computation tests pass and a test proves a moved-load-only change is not reported as dependency loss
- [x] 4.046 Write failing tests for error paths: Graphify binary missing and nonzero stage exit code with stderr capture | **Status:** DONE | **DoD:** Tests asserting GraphifyNotFoundError and GraphifyStageError (with stage name and stderr text) fail red
- [x] 4.047 Implement GraphifyNotFoundError and GraphifyStageError handling in the wrapper and orchestrator without crashing the agent loop | **Status:** DONE | **DoD:** Error-path tests pass and orchestrator returns a failed StageResult instead of an unhandled exception
- [x] 4.048 Write failing tests for SDK facade methods run_graphify_pipeline, parse_graph, and diff_graphs in sdk/sdk.py | **Status:** DONE | **DoD:** SDK facade tests fail red asserting the three methods exist and delegate to graphops
- [x] 4.049 Implement the SDK facade methods so all Graphify business logic is reachable only via sdk/sdk.py | **Status:** DONE | **DoD:** SDK tests pass and grep shows no graphops import in src/main.py or agents bypassing the SDK
- [x] 4.050 Verify Phase 4 quality gates across all graphops code and tests | **Status:** DONE | **DoD:** `uv run ruff check` reports 0 violations, `uv run pytest --cov` shows coverage ≥85%, and every graphops file is ≤150 code lines

## Phase 5 — Obsidian Vault & Navigation Pages

- [x] 5.001 Review and extend the Obsidian vault sections of docs/PRD_graph_pipeline.md (vault layout, hot.md/index.md/wiki page specs, mermaid folder diagram, FR-xx IDs) | **Status:** DONE | **DoD:** Vault sections of docs/PRD_graph_pipeline.md contain at least 8 FR-xx requirements covering hot.md, index.md, wiki/, raw/, log.md.
- [ ] 5.002 Obtain lecturer approval of the vault sections of docs/PRD_graph_pipeline.md before vault development starts | **Status:** BLOCKED | **DoD:** Status line in PRD_graph_pipeline.md changed from Draft to Approved with approval date recorded.
- [x] 5.003 Configure vault settings block in config/setup.json (vault_root, raw_dir_name, wiki_dir_name, hot_top_n=10, index_read_first_count) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_config_keys.py` exits 0 asserting all five keys load via cfg.get with no defaults hardcoded in vault modules.
- [x] 5.004 Add vault constants to src/archlens/shared/constants.py (HOT_MD, INDEX_MD, LOG_MD, RAW_DIR, WIKI_DIR, FRONTMATTER_KEYS=type/status/project) | **Status:** DONE | **DoD:** Grep for literal "hot.md", "index.md", "log.md" under src/archlens/vault returns 0 hits outside constants imports.
- [x] 5.005 Write tests/vault/conftest.py providing a fixture Graphify graph.json (>=12 nodes, >=2 communities, >=1 AMBIGUOUS edge with confidence in 0.55-0.95) and a tmp_path vault factory | **Status:** DONE | **DoD:** `uv run pytest tests/vault --collect-only` exits 0 and lists the fixtures.
- [x] 5.006 Write failing tests for the vault layout builder asserting raw/ and wiki/ directories are created under the configured vault_root | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_layout.py` fails red with ModuleNotFoundError for archlens.vault.layout.
- [x] 5.007 Implement src/archlens/vault/layout.py creating the vault skeleton (vault_root, raw/, wiki/) from config values only | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_layout.py` exits 0.
- [x] 5.008 Write failing tests for the YAML frontmatter renderer requiring type, status, and project keys on every generated note | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_frontmatter.py` fails red before implementation.
- [x] 5.009 Implement src/archlens/vault/frontmatter.py rendering a YAML frontmatter block with type/status/project sourced from config and constants | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_frontmatter.py` exits 0 with a test asserting output starts with a `---` block containing all three keys.
- [x] 5.010 Write failing tests for the wikilink builder (slugify node and community names with spaces, dots, and case collisions into valid [[Page]] links) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_wikilinks.py` fails red before implementation.
- [x] 5.011 Implement src/archlens/vault/wikilinks.py with slugify and render_link functions reused by all page generators | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_wikilinks.py` exits 0 including the collision-disambiguation test case.
- [x] 5.012 Write failing tests for the one-idea-per-note linter (reject notes with more than one H1 or more than one top-level concept section) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_note_lint.py` fails red before implementation.
- [x] 5.013 Implement src/archlens/vault/note_lint.py enforcing one-idea-per-note on every note before it is written to disk | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_note_lint.py` exits 0 with both accept and reject cases green.
- [x] 5.014 Write failing tests for the hot.md top-10 centrality table (descending score order, node names as wikilinks, top_n read from config) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_hot_page.py::test_centrality_table` fails red before implementation.
- [x] 5.015 Implement the centrality section of src/archlens/vault/hot_page.py reading degree/betweenness scores from graph.json | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_hot_page.py::test_centrality_table` exits 0.
- [x] 5.016 Write failing tests for the hot.md entry-points section (detected main/CLI entry nodes listed with wikilinks) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_hot_page.py::test_entry_points` fails red before implementation.
- [x] 5.017 Implement the entry-points section of hot_page.py sourcing entry nodes from the graph.json node metadata | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_hot_page.py::test_entry_points` exits 0.
- [x] 5.018 Write failing tests for the hot.md AMBIGUOUS-edges-needing-review section with relation, confidence (0.55-0.95), and source_file columns | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_hot_page.py::test_ambiguous_edges` fails red before implementation.
- [x] 5.019 Implement the AMBIGUOUS-edges section of hot_page.py filtering edge triage status AMBIGUOUS and rendering relation/confidence/source_file rows | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_hot_page.py::test_ambiguous_edges` exits 0.
- [x] 5.020 Write a golden-file test comparing generated hot.md against the PRD_graph_pipeline sample layout (timestamp line excluded) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_hot_page.py::test_golden_file` exits 0 with byte-identical comparison outside the timestamp line.
- [x] 5.021 Write failing tests for the index.md hub generator (read-first banner stating index-then-2-3-pages rule, exactly 2-3 curated next-read wikilinks) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_index_page.py` fails red before implementation.
- [x] 5.022 Implement src/archlens/vault/index_page.py generating the hub note with read-first instructions and curated links to hot.md plus top communities | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_index_page.py` exits 0 including the 2-3-link count assertion.
- [x] 5.023 Write failing tests for the wiki/ page-per-community generator (one .md per community, member table, internal/external edge counts) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_community_pages.py` fails red before implementation.
- [x] 5.024 Implement src/archlens/vault/community_pages.py emitting one frontmattered note per community with wikilinks to member and neighbor pages | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_community_pages.py` exits 0 with file count equal to fixture community count.
- [x] 5.025 Write a failing reachability test asserting every wiki/ page is reachable from index.md via wikilinks within 2 hops | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_reachability.py` fails red before cross-link implementation.
- [x] 5.026 Implement cross-link injection (community pages link back to index.md and hot.md; index links forward to all community pages) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_reachability.py` exits 0.
- [x] 5.027 Write failing tests for the log.md ingestion journal appender (append-only, ISO-8601 timestamped entries, existing content never truncated) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_log_journal.py` fails red before implementation.
- [x] 5.028 Implement src/archlens/vault/log_journal.py appending one journal entry per ingestion event with timestamp, action, and source file | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_log_journal.py` exits 0 including the no-truncation regression test.
- [x] 5.029 Write failing tests for the raw/ ingestion copier (graph.json and REPORT.md copied verbatim from Graphify output into raw/) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_raw_ingest.py` fails red before implementation.
- [x] 5.030 Implement src/archlens/vault/raw_ingest.py copying Graphify artifacts into raw/ without modification and logging each copy to log.md | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_raw_ingest.py` exits 0 with SHA-256 checksum equality asserted between source and raw/ copies.
- [x] 5.031 Test raw/ vs wiki/ separation by asserting no wiki/ note wikilinks into raw/ and no generator ever writes into raw/ except raw_ingest | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_separation.py` exits 0 on the fully built fixture vault.
- [x] 5.032 Write failing tests for the orphan-page detector (wiki/ note with zero inbound wikilinks is reported) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_validate.py::test_orphans` fails red before implementation.
- [x] 5.033 Implement orphan-page detection in src/archlens/vault/validate.py | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_validate.py::test_orphans` exits 0 for both clean and seeded-orphan vaults.
- [x] 5.034 Write failing tests for the broken-wikilink detector ([[Target]] without a matching note file is reported with source note and link text) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_validate.py::test_broken_links` fails red before implementation.
- [x] 5.035 Implement broken-wikilink detection in src/archlens/vault/validate.py | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_validate.py::test_broken_links` exits 0 for both clean and seeded-broken-link vaults.
- [x] 5.036 Implement the vault validation report aggregator returning a structured result (orphans, broken links, note-lint violations) with parametrized seeded-bad-vault tests | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_validate.py::test_report` exits 0 asserting zero violations on the clean fixture and one entry per seeded violation type.
- [x] 5.037 Write a failing integration test for the full VaultBuilder pipeline (layout -> raw_ingest -> community_pages -> hot -> index -> log -> validate) from the fixture graph.json | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_builder.py` fails red before implementation.
- [x] 5.038 Implement src/archlens/vault/builder.py orchestrating all generators behind a single build_vault function | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_builder.py` exits 0 producing hot.md, index.md, log.md, raw/, and wiki/ in one call.
- [x] 5.039 Write a failing determinism test (second build yields byte-identical wiki/, hot.md, and index.md while log.md grows by exactly one entry) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_builder.py::test_idempotent_rebuild` fails red before implementation.
- [x] 5.040 Implement idempotent rebuild behavior in builder.py (stable ordering, overwrite wiki/, append-only log.md) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_builder.py::test_idempotent_rebuild` exits 0.
- [x] 5.041 Write a failing SDK test asserting sdk.build_vault() is the only public entry to vault business logic | **Status:** DONE | **DoD:** `uv run pytest tests/sdk/test_build_vault.py` fails red before implementation.
- [x] 5.042 Expose build_vault through src/archlens/sdk/sdk.py delegating to vault.builder with config injection | **Status:** DONE | **DoD:** `uv run pytest tests/sdk/test_build_vault.py` exits 0.
- [x] 5.043 Wire a thin `vault` subcommand into src/main.py that only parses args and calls sdk.build_vault | **Status:** DONE | **DoD:** `uv run archlens vault --help` exits 0 and src/main.py contains zero vault business logic on review sign-off.
- [x] 5.044 Integrate GraphAgent to invoke sdk.build_vault immediately after the Graphify export step in the LangGraph flow | **Status:** DONE | **DoD:** `uv run pytest tests/agents/test_graph_agent_vault.py` exits 0 asserting vault files exist after the GraphAgent node runs on the fixture.
- [x] 5.045 Write the Obsidian-open smoke instructions section in docs/PRD_graph_pipeline.md (open vault as folder, check graph view renders communities, unresolved-links pane empty) | **Status:** DONE | **DoD:** Section exists with numbered, executable steps and expected-result statement for each step.
- [ ] 5.046 Execute the Obsidian-open smoke test on a generated vault and append the verdict to log.md | **Status:** BLOCKED (Obsidian GUI unavailable in this headless environment; programmatic link validation via validate_vault passes with zero broken links/orphans on a clean build) | **DoD:** log.md contains a dated smoke-test entry with PASS verdict and zero unresolved links reported by Obsidian.
- [x] 5.047 Record before/after knowledge-asset measurements for the vault (source traceability, noise reduction, correct-file identification, correct-tool-at-the-right-time) in log.md | **Status:** DONE | **DoD:** log.md contains a 4-row before/after table with a numeric or boolean value in every cell.
- [x] 5.048 Verify the 150-line cap on every file under src/archlens/vault and tests/vault (blank and comment lines excluded) | **Status:** DONE | **DoD:** The repo line-count check script reports 0 violations for the vault and tests/vault paths.
- [x] 5.049 Verify Ruff compliance for the vault feature | **Status:** DONE | **DoD:** `uv run ruff check src/archlens/vault tests/vault` exits 0 with zero violations.
- [x] 5.050 Verify coverage of the vault package meets the project gate | **Status:** DONE | **DoD:** `uv run pytest tests/vault --cov=archlens.vault --cov-branch --cov-fail-under=85` exits 0.

## Phase 6 — Graph Analysis Engine

- [x] 6.001 Write failing unit tests in tests/graphops/test_loader.py for loading a Graphify graph.json into a networkx DiGraph preserving node ids and the edge attributes type, confidence, and source_file | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_loader.py` exits non-zero with all new tests collected (red state)
- [x] 6.002 Implement load_graph() in src/archlens/graphops/loader.py returning a networkx DiGraph with all node and edge attributes intact | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_loader.py` passes with 0 failures
- [x] 6.003 Write failing loader-validation tests rejecting edges with missing source_file, confidence outside 0.0-1.0, or unknown relation type | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_loader.py -k schema` exits non-zero with the new tests collected (red state)
- [x] 6.004 Implement GraphSchemaError validation in the loader listing every offending node or edge id in the exception message | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_loader.py` passes with 0 failures
- [x] 6.005 Implement synthetic fixture builders in tests/graphops/fixtures.py (star, chain, barbell, two_community, auth_path, composite) with known answers documented in each builder docstring plus a smoke test of node and edge counts | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_fixtures.py` passes with 0 failures
- [x] 6.006 Write failing tests requiring every analysis result DTO to be an immutable dataclass exposing relation, confidence, and source_file citation fields | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_dto.py` exits non-zero with the new tests collected (red state)
- [x] 6.007 Implement frozen dataclass DTOs (CentralityRow, HubBottleneckVerdict, BridgeReport, SpofFinding, TriageItem, DuplicatePair, ReviewItem) in src/archlens/graphops/dto.py | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_dto.py` passes with 0 failures
- [x] 6.008 Write failing degree-centrality tests on the star fixture asserting hub degree n-1 and leaf degree 1 for the in, out, and total variants | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_centrality.py -k degree` exits non-zero with the new tests collected (red state)
- [x] 6.009 Implement degree centrality (in, out, total) in src/archlens/graphops/centrality.py returning ranked CentralityRow DTOs | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_centrality.py -k degree` passes with 0 failures
- [x] 6.010 Write failing betweenness-centrality tests for the barbell fixture using hand-computed expected values with the manual shortest-path calculation documented in the test docstring | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_centrality.py -k betweenness` exits non-zero with the new tests collected (red state)
- [x] 6.011 Implement a betweenness-centrality wrapper over networkx.betweenness_centrality matching the hand-verified fixture values within tolerance 1e-9 | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_centrality.py -k betweenness` passes with 0 failures
- [x] 6.012 Write failing community-detection tests on the two_community fixture asserting the exact known two-block partition | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_communities.py -k detect` exits non-zero with the new tests collected (red state)
- [x] 6.013 Implement community detection via networkx greedy modularity in src/archlens/graphops/communities.py | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_communities.py -k detect` passes with 0 failures
- [x] 6.014 Write failing COMMUNITY!=FOLDER tests deriving each node folder from its source_file path and asserting mismatched nodes are listed with both community id and folder | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_communities.py -k folder` exits non-zero with the new tests collected (red state)
- [x] 6.015 Implement the community-vs-folder comparator producing one mismatch DTO per node whose community differs from its folder grouping | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_communities.py -k folder` passes with 0 failures
- [x] 6.016 Write failing tests for the modularity score and inter-community edge count with known expected values on the two_community fixture | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_graph_metrics.py` exits non-zero with the new tests collected (red state)
- [x] 6.017 Implement modularity and inter-community edge-count metrics in src/archlens/graphops/graph_metrics.py feeding the Part C improvement-loop diff stop conditions | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_graph_metrics.py` passes with 0 failures
- [x] 6.018 Write failing alternative-path tests asserting node-disjoint bypass path count >= 2 on the healthy-hub fixture and 0 on the bottleneck fixture | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_classify.py -k alternative` exits non-zero with the new tests collected (red state)
- [x] 6.019 Implement the alternative-path analyzer (node-disjoint path counting plus removal-connectivity check) in src/archlens/graphops/classify.py | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_classify.py -k alternative` passes with 0 failures
- [x] 6.020 Write failing hub-vs-bottleneck classifier tests labeling the hub fixture HUB (alternative paths exist) and the bottleneck fixture BOTTLENECK (mandatory path) with a rationale citing degree, betweenness, and bypass count | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_classify.py -k verdict` exits non-zero with the new tests collected (red state)
- [x] 6.021 Implement the hub-vs-bottleneck classifier emitting HubBottleneckVerdict DTOs that combine centrality scores with the alternatives analysis | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_classify.py` passes with 0 failures
- [x] 6.022 Write failing structural-bridge tests asserting edges with no redundant alternate path (removal disconnects the undirected projection) are detected on the barbell fixture | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_bridges.py -k structural` exits non-zero with the new tests collected (red state)
- [x] 6.023 Implement structural bridge detection using networkx bridges on the undirected projection in src/archlens/graphops/bridges.py | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_bridges.py -k structural` passes with 0 failures
- [x] 6.024 Write failing community-connector tests asserting edges linking distinct communities are flagged and returned in a list separate from structural bridges | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_bridges.py -k connector` exits non-zero with the new tests collected (red state)
- [x] 6.025 Implement community-connector detection and a BridgeReport DTO with separate structural and connector sections so the two bridge senses are never merged | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_bridges.py` passes with 0 failures
- [x] 6.026 Write failing critical-path tests on the auth_path fixture extracting the login chain across validates, writes_session, and checks_policy edges | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_spof.py -k critical_path` exits non-zero with the new tests collected (red state)
- [x] 6.027 Define the critical relation types validates, writes_session, and checks_policy as an Enum in src/archlens/shared/constants.py and reference only the Enum from graphops code | **Status:** DONE | **DoD:** a grep for the three literal relation strings inside src/archlens/graphops/ returns zero matches
- [x] 6.028 Implement critical-path extraction in src/archlens/graphops/spof.py returning only paths composed entirely of critical edges | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_spof.py -k critical_path` passes with 0 failures
- [x] 6.029 Write failing SPOF-detector tests asserting removal of the session-store node breaks every critical path and each finding cites relation, confidence, and source_file for every hop | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_spof.py -k detector` exits non-zero with the new tests collected (red state)
- [x] 6.030 Implement the SPOF detector emitting SpofFinding DTOs with the full per-hop relation, confidence, and source_file citation chain | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_spof.py` passes with 0 failures
- [x] 6.031 Write failing edge-triage tests asserting every edge of the composite fixture is bucketed as EXTRACTED, INFERRED, or AMBIGUOUS by relation type with the expected per-bucket counts | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_triage.py -k buckets` exits non-zero with the new tests collected (red state)
- [x] 6.032 Implement edge triage in src/archlens/graphops/triage.py returning TriageItem DTOs grouped by EXTRACTED, INFERRED, and AMBIGUOUS buckets | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_triage.py -k buckets` passes with 0 failures
- [x] 6.033 Write failing confidence-policy tests asserting confidence at the 0.55 floor is flagged needs_validation, values >= 0.95 are labeled strong, and values below 0.55 are routed to human review | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_triage.py -k confidence` exits non-zero with the new tests collected (red state)
- [x] 6.034 Implement the confidence-threshold policy reading the 0.55 floor and 0.95 strong thresholds from config/setup.json via cfg.get with no numeric threshold literals in graphops code | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_triage.py -k confidence` passes with 0 failures
- [x] 6.035 Write failing human-review queue tests asserting AMBIGUOUS and sub-floor edges are enqueued in FIFO order and survive a save-then-load round trip | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_review_queue.py` exits non-zero with the new tests collected (red state)
- [x] 6.036 Implement the AMBIGUOUS-edge human-review queue in src/archlens/graphops/review_queue.py exporting review_queue.json with ReviewItem entries (edge, reason, confidence, source_file) | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_review_queue.py` passes with 0 failures
- [x] 6.037 Write failing duplicate-triage boundary tests asserting a pair scored 0.91 is flagged and a pair scored 0.909 is ignored | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_duplicates.py -k boundary` exits non-zero with the new tests collected (red state)
- [x] 6.038 Implement the duplicate similarity scorer producing DuplicatePair DTOs only for pairs with similarity >= 0.91 read from config | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_duplicates.py -k boundary` passes with 0 failures
- [x] 6.039 Write failing never-auto-merge tests asserting duplicate findings produce only review-queue items and that no merge action object is ever emitted | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_duplicates.py -k never_merge` exits non-zero with the new tests collected (red state)
- [x] 6.040 Implement the never-auto-merge guard routing every DuplicatePair into the human-review queue instead of any merge operation | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_duplicates.py` passes with 0 failures
- [x] 6.041 Write failing macro-view tests asserting node count, edge count, density, component count, and the top-5 hubs on the composite fixture | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_views.py -k macro` exits non-zero with the new tests collected (red state)
- [x] 6.042 Implement the macro reading API in src/archlens/graphops/views.py returning the whole-graph summary DTO | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_views.py -k macro` passes with 0 failures
- [x] 6.043 Write failing meso-view tests asserting per-community subgraph summaries that include each community's connector edges | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_views.py -k meso` exits non-zero with the new tests collected (red state)
- [x] 6.044 Implement the meso reading API returning one community summary DTO per detected community | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_views.py -k meso` passes with 0 failures
- [x] 6.045 Write failing micro-view tests asserting a single-node neighborhood view where every edge carries its relation, confidence, and source_file citation | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_views.py -k micro` exits non-zero with the new tests collected (red state)
- [x] 6.046 Implement the micro reading API returning the cited neighborhood DTO for any requested node id | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_views.py` passes with 0 failures
- [x] 6.047 Write failing tests for the query command (filter nodes and edges by attribute) and the path command (shortest path with per-edge citations) on the composite fixture | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_commands.py -k "query or path"` exits non-zero with the new tests collected (red state)
- [x] 6.048 Implement the query and path commands in src/archlens/graphops/commands.py returning DTO lists with citations | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_commands.py -k "query or path"` passes with 0 failures
- [x] 6.049 Write failing tests for the explain command (why an edge exists: relation, confidence, source_file) and the diff command (before-vs-after deltas covering bottleneck dependency loss, inter-community edges, and isolated components) | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_commands.py -k "explain or diff"` exits non-zero with the new tests collected (red state)
- [x] 6.050 Implement the explain and diff commands returning DTOs consumed by the improvement-loop stop-condition check | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_commands.py` passes with 0 failures
- [x] 6.051 Write failing SDK facade tests asserting every Phase 6 analysis operation is reachable through ArchLensSDK methods including one end-to-end analysis run on the composite fixture | **Status:** DONE | **DoD:** `uv run pytest tests/test_sdk_graph_analysis.py` exits non-zero with the new tests collected (red state)
- [x] 6.052 Implement the Phase 6 analysis facade methods in src/archlens/sdk/sdk.py delegating to graphops modules so no caller imports graphops directly | **Status:** DONE | **DoD:** `uv run pytest tests/test_sdk_graph_analysis.py` passes with 0 failures
- [x] 6.053 Verify every file under src/archlens/graphops/ and tests/graphops/ stays within the 150 effective-line cap (blank and comment lines excluded), splitting any offender into cohesive modules | **Status:** DONE | **DoD:** the repository line-cap checker reports 0 violations for src/archlens/graphops/ and tests/graphops/
- [x] 6.054 Verify Ruff cleanliness for all Phase 6 code and tests under the project rule set (E, F, W, I, N, UP, B, C4, SIM) | **Status:** DONE | **DoD:** `uv run ruff check src/archlens/graphops tests/graphops` reports 0 violations
- [x] 6.055 Verify test coverage for the graphops package meets the project gate including branch coverage | **Status:** DONE | **DoD:** `uv run pytest tests/graphops --cov=src/archlens/graphops --cov-branch --cov-fail-under=85` exits 0

## Phase 7 — Reverse-Engineering Deliverables

- [x] 7.001 Define deliverable-output constants (DELIVERABLES_DIR, ARCHITECTURE_MD, CLASS_SCHEMA_MD, ALIGNMENT_AUDIT_MD, EVIDENCE_TAGS) in src/archlens/shared/constants.py | **Status:** DONE | **DoD:** `uv run python -c "from archlens.shared.constants import DELIVERABLES_DIR, EVIDENCE_TAGS"` exits 0
- [x] 7.002 Configure a "deliverables" section in config/setup.json (output dir, mermaid direction, match-confidence threshold) so no value is hardcoded; the duplicate-similarity threshold stays single-sourced in the analysis block (DRY) | **Status:** DONE | **DoD:** `uv run python -c "import json; assert json.load(open('config/setup.json'))['deliverables']"` exits 0
- [x] 7.003 Add failing unit tests for the graph.json community loader (cluster ids, member modules, inter-community edges) to the existing tests/graphops/test_communities.py | **Status:** DONE | **DoD:** the new load_communities tests are collected and fail (red) before 7.004 while the Phase 6 community-detection tests in the same file stay green
- [x] 7.004 Implement the community loader in src/archlens/graphops/communities.py parsing Graphify graph.json cluster output | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_communities.py` passes with 0 failures
- [x] 7.005 Write failing unit tests for the block-model builder mapping each community to a named block with aggregated inter-block edge weights | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_block_model.py` collects >=3 tests and exits non-zero (red phase)
- [x] 7.006 Implement src/archlens/graphops/block_model.py building blocks (name, member count, directed weighted inter-block edges) from loaded communities | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_block_model.py` passes with 0 failures
- [x] 7.007 Write failing unit tests for the mermaid flowchart renderer asserting a fenced mermaid block with one node per block and one edge per inter-block dependency | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_mermaid_blocks.py` collects >=3 tests and exits non-zero (red phase)
- [x] 7.008 Implement src/archlens/graphops/mermaid_blocks.py rendering the block model as a fenced mermaid flowchart honoring the configured direction | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_mermaid_blocks.py` passes with 0 failures
- [x] 7.009 Test the block-diagram pipeline end-to-end on a fixture graph.json verifying every community appears exactly once as a mermaid node | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_block_diagram_integration.py` passes with 0 failures
- [x] 7.010 Expose generate_block_diagram() in src/archlens/sdk/sdk.py as the single entry point for block-diagram generation, with an SDK-level test | **Status:** DONE | **DoD:** `uv run pytest tests/sdk/test_sdk_block_diagram.py` passes with 0 failures
- [x] 7.011 Write failing unit tests for the AST class extractor (class names, methods, attributes per module) in tests/graphops/test_class_extractor.py | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_class_extractor.py` collects >=4 tests and exits non-zero (red phase)
- [x] 7.012 Implement src/archlens/graphops/class_extractor.py extracting classes, methods, and attributes from target-repo sources via the ast module | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_class_extractor.py` passes with 0 failures
- [x] 7.013 Write failing unit tests for inheritance and composition detection (base classes including dotted names; composition via attribute instantiation and type annotations) | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_class_relations.py` collects >=4 tests and exits non-zero (red phase)
- [x] 7.014 Implement src/archlens/graphops/class_relations.py resolving inheritance and composition edges between extracted classes | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_class_relations.py` passes with 0 failures
- [x] 7.015 Write failing unit tests for the mermaid classDiagram renderer asserting the classDiagram header, inheritance arrows (<|--), and composition arrows (*--) | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_mermaid_classes.py` collects >=3 tests and exits non-zero (red phase)
- [x] 7.016 Implement src/archlens/graphops/mermaid_classes.py rendering classes and relations as a fenced mermaid classDiagram | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_mermaid_classes.py` passes with 0 failures
- [x] 7.017 Expose extract_class_schema() in src/archlens/sdk/sdk.py returning the class model plus rendered classDiagram, with an SDK-level test on a multi-file fixture package | **Status:** DONE | **DoD:** `uv run pytest tests/sdk/test_sdk_class_schema.py` passes with 0 failures
- [x] 7.018 Write failing unit tests for the FR-xx/NFR-xx requirement parser over PRD markdown fixtures (ids, titles, section context) | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_req_parser.py` collects >=3 tests and exits non-zero (red phase)
- [x] 7.019 Implement src/archlens/graphops/req_parser.py extracting requirement ids and titles from docs/PRD.md-style markdown | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_req_parser.py` passes with 0 failures
- [x] 7.020 Write failing unit tests for the requirement-to-module matcher producing (req_id, module, confidence) tuples bounded to 0.55-0.95 | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_req_matcher.py` collects >=4 tests and exits non-zero (red phase)
- [x] 7.021 Implement src/archlens/graphops/req_matcher.py matching requirements to graph modules via symbol and keyword evidence using the config-driven confidence threshold | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_req_matcher.py` passes with 0 failures
- [x] 7.022 Write failing unit tests for the unimplemented-requirement detector (requirement with zero module matches above threshold) | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_gap_detector.py` collects >=3 tests and exits non-zero (red phase)
- [x] 7.023 Implement src/archlens/graphops/gap_detector.py listing requirements never implemented, each with the evidence that was searched | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_gap_detector.py` passes with 0 failures
- [x] 7.024 Write failing unit tests for the orphan-module detector (graph module mapped to zero requirements) | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_orphan_detector.py` collects >=3 tests and exits non-zero (red phase)
- [x] 7.025 Implement src/archlens/graphops/orphan_detector.py reporting modules with no matched requirement, sorted by node degree | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_orphan_detector.py` passes with 0 failures
- [x] 7.026 Write failing unit tests for the shared-flow detector flagging modules serving 2+ requirements and duplicate flows at similarity >= 0.91 | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_flow_detector.py` collects >=3 tests and exits non-zero (red phase)
- [x] 7.027 Implement src/archlens/graphops/flow_detector.py detecting shared flows and duplicate-logic candidates against the configured 0.91 similarity threshold | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_flow_detector.py` passes with 0 failures
- [x] 7.028 Expose run_alignment_audit() in src/archlens/sdk/sdk.py aggregating gaps, orphans, and shared flows into one audit result, with an SDK-level test | **Status:** DONE | **DoD:** `uv run pytest tests/sdk/test_sdk_alignment_audit.py` passes with 0 failures
- [x] 7.029 Write failing unit tests for the PRD->Module->Test traceability chain builder linking each requirement to matched modules and to pytest files referencing them | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_traceability.py` collects >=4 tests and exits non-zero (red phase)
- [x] 7.030 Implement src/archlens/graphops/traceability.py building full PRD->Module->Test chains with per-link confidence values | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_traceability.py` passes with 0 failures
- [x] 7.031 Write failing unit tests for the verdict generator asserting 3-4 sentence length, citation of relation -> confidence -> source_file, and a verdict label of "full traceability" or "possible gap" | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_verdicts.py` collects >=4 tests and exits non-zero (red phase)
- [x] 7.032 Implement src/archlens/graphops/verdicts.py emitting one evidence-grounded 3-4 sentence verdict per requirement chain | **Status:** DONE | **DoD:** `uv run pytest tests/graphops/test_verdicts.py` passes with 0 failures
- [x] 7.033 Write failing unit tests for the evidence-ladder language linter rejecting any claim sentence lacking an OBSERVED, INFERRED, EXTRACTED, or VALIDATED tag | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_evidence_lint.py` collects >=4 tests and exits non-zero (red phase)
- [x] 7.034 Implement src/archlens/vault/evidence_lint.py validating that every claim in deliverable text carries exactly one evidence-ladder tag | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_evidence_lint.py` passes with 0 failures
- [x] 7.035 Integrate the evidence linter as a generation gate so deliverable rendering raises a typed error on any untagged claim | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_evidence_gate.py` passes, including a test asserting generation aborts on an untagged claim
- [x] 7.036 Write failing unit tests for the ARCHITECTURE.md generator (house-style version header, embedded mermaid block diagram, community table, evidence-tagged narrative) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_arch_doc.py` collects >=3 tests and exits non-zero (red phase)
- [x] 7.037 Implement src/archlens/vault/arch_doc.py writing deliverables/ARCHITECTURE.md from the block model via the SDK pipeline | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_arch_doc.py` passes and the fixture run produces deliverables/ARCHITECTURE.md containing a fenced mermaid flowchart
- [x] 7.038 Write failing unit tests for the CLASS_SCHEMA.md generator (version header, mermaid classDiagram, inheritance and composition tables) | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_class_doc.py` collects >=3 tests and exits non-zero (red phase)
- [x] 7.039 Implement src/archlens/vault/class_doc.py writing deliverables/CLASS_SCHEMA.md from the extracted class model | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_class_doc.py` passes and the fixture run produces deliverables/CLASS_SCHEMA.md containing a fenced mermaid classDiagram
- [x] 7.040 Write failing unit tests for the ALIGNMENT_AUDIT.md generator covering sections Unimplemented Requirements, Orphan Modules, Shared Flows, and Traceability Verdicts | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_audit_doc.py` collects >=4 tests and exits non-zero (red phase)
- [x] 7.041 Implement src/archlens/vault/audit_doc.py writing deliverables/ALIGNMENT_AUDIT.md with all four audit sections and per-claim evidence tags | **Status:** DONE | **DoD:** `uv run pytest tests/vault/test_audit_doc.py` passes and the fixture run produces deliverables/ALIGNMENT_AUDIT.md containing all four section headings
- [x] 7.042 Wire a "deliverables" subcommand into the thin CLI in src/main.py that only delegates to the SDK with zero business logic | **Status:** DONE | **DoD:** `uv run archlens deliverables` against the fixture exits 0 and writes ARCHITECTURE.md, CLASS_SCHEMA.md, and ALIGNMENT_AUDIT.md under deliverables/
- [x] 7.043 Write deliverables/REVIEW_CHECKLIST.md human-review checklist covering diagram accuracy, verdict evidence spot checks, evidence-tag sampling, and reviewer/date sign-off fields | **Status:** DONE | **DoD:** deliverables/REVIEW_CHECKLIST.md exists with >=10 numbered checklist items and a sign-off section
- [x] 7.044 Verify Phase 7 quality gates across all new modules and tests (coverage, lint, file-length limit) | **Status:** DONE | **DoD:** the gate chain `uv run pytest --cov --cov-fail-under=85`, `uv run ruff check .`, and the 150-line checker all exit 0
- [ ] 7.045 Obtain lecturer approval of the reverse-engineering deliverables (block diagram, class schema, alignment audit) | **Status:** BLOCKED | **DoD:** written lecturer approval with date is recorded in docs/TODO.md next to Phase 7

## Phase 8 — SDK Layer & Core Architecture

- [x] 8.001 Analyze MATERIALS course sources (Guidelines V3, L07 §11, Part B) for SDK-layer rules (single entry point, gatekeeper chokepoint, DRY thresholds, 150-line cap) | **Status:** DONE | **DoD:** MATERIALS/ directory present in the repo and every extracted SDK rule traceable to a Phase 8 task ID in this section
- [x] 8.002 Review and approve the SDK facade architecture section in docs/PLAN.md §3 (SDK facade requirements and mermaid component diagram) | **Status:** IN_PROGRESS | **DoD:** PLAN.md SDK facade architecture section contains the facade requirement statements and a fenced mermaid component block, with review notes recorded in the PLAN.md change log
- [x] 8.003 Verify the SDK-related ADR coverage in the canonical docs/PLAN.md §10 ADR set (ADR-003 SDK single entry) addresses the facade pattern, gatekeeper dependency injection, and plugin registry decisions | **Status:** IN_PROGRESS | **DoD:** ADR-003 in the PLAN.md §10 ADR index covers all three decisions with Context/Decision/Consequences and a Status field
- [ ] 8.004 Submit the docs/PLAN.md SDK facade architecture section (§3) to the lecturer for approval before Phase 8 implementation starts | **Status:** BLOCKED | **DoD:** Lecturer sign-off on the PLAN.md SDK facade section recorded in docs/approvals/ with date
- [x] 8.005 Configure an "sdk" block in config/setup.json (default_analysis_depth, plugin_allowlist, vault_output_root) and define the 5-iteration hard cap as MAX_LOOP_ITERATIONS in src/archlens/shared/constants.py (not config/setup.json) | **Status:** DONE | **DoD:** All three sdk-block keys readable via cfg.get in a passing unit test with no hardcoded fallbacks in src, and `uv run python -c "from archlens.shared.constants import MAX_LOOP_ITERATIONS; assert MAX_LOOP_ITERATIONS == 5"` exits 0
- [x] 8.006 Add SDK-layer constants (CLI subcommand names, plugin group name, exception code prefixes, DTO schema version) to shared/constants.py | **Status:** DONE | **DoD:** Constants imported by sdk modules and `uv run ruff check src` reports 0 violations
- [x] 8.007 Write failing unit tests for the exception taxonomy (ArchLensError base; Config/Repo/Graphify/Analysis/Refactor/QAGate/Gatekeeper/TokenBudget subclasses with code and source_context attributes) | **Status:** DONE | **DoD:** `uv run pytest tests/test_exceptions.py` fails red with the classes not yet implemented
- [x] 8.008 Implement the exception taxonomy in src/archlens/shared/exceptions.py with machine-readable error codes from constants.py | **Status:** DONE | **DoD:** All tests in tests/test_exceptions.py pass and the file is ≤150 code lines
- [x] 8.009 Write failing unit tests for DTO dataclasses (RepoSpec, GraphArtifacts, AnalysisReport, BugFinding, RefactorPlan, LoopResult, TokenReport) covering required fields and type validation | **Status:** DONE | **DoD:** DTO tests fail red against an empty sdk dto module
- [x] 8.010 Implement frozen dataclasses RepoSpec, GraphArtifacts, and AnalysisReport in src/archlens/sdk/dto_core.py | **Status:** DONE | **DoD:** dto_core tests pass and the file is ≤150 code lines
- [x] 8.011 Implement frozen dataclasses BugFinding, RefactorPlan, LoopResult, and TokenReport in src/archlens/sdk/dto_loop.py | **Status:** DONE | **DoD:** dto_loop tests pass and the file is ≤150 code lines
- [x] 8.012 Write failing tests for DTO JSON round-trip serialization (to_dict/from_dict on every DTO with schema_version stamped) | **Status:** DONE | **DoD:** Round-trip tests fail red asserting a serializer that does not yet exist
- [x] 8.013 Implement one shared serialization helper in src/archlens/shared/serde.py reused by both DTO modules (DRY: 2+ files → shared module) | **Status:** DONE | **DoD:** Round-trip tests pass and no per-DTO duplicate to_dict implementation remains in src
- [x] 8.014 Write failing tests for GatekeeperProtocol conformance (call_llm, run_subprocess, http_get signatures plus rate-limit hook) | **Status:** DONE | **DoD:** Protocol tests fail red with interfaces module absent
- [x] 8.015 Define GatekeeperProtocol as a typing.Protocol in src/archlens/shared/interfaces.py | **Status:** DONE | **DoD:** Conformance tests pass and `uv run ruff check src` reports 0 violations
- [x] 8.016 Write failing dependency-injection tests asserting all seven agent classes (Repo, Graph, Analyst, BugHunter, Refactor, QA, Metrics) require a gatekeeper constructor argument | **Status:** DONE | **DoD:** DI tests fail red against current agent constructors
- [x] 8.017 Refactor all seven agent constructors to accept the injected gatekeeper and delete any module-level client instantiation | **Status:** DONE | **DoD:** DI tests pass and grep for external client/subprocess imports in src/archlens/agents returns 0 matches
- [x] 8.018 Write failing tests for ArchLensSDK construction (loads config/setup.json via cfg.get, builds exactly one gatekeeper instance, lazily wires agents) | **Status:** DONE | **DoD:** Construction tests fail red with sdk.py not yet implemented
- [x] 8.019 Implement the ArchLensSDK facade skeleton in src/archlens/sdk/sdk.py as the single public entry point for all business logic | **Status:** DONE | **DoD:** Construction tests pass and the file is ≤150 code lines
- [x] 8.020 Write failing tests for sdk.analyze() orchestrating RepoAgent → GraphAgent → AnalystAgent through a mocked LangGraph supervisor and returning an AnalysisReport DTO | **Status:** DONE | **DoD:** analyze() tests fail red asserting the missing method
- [x] 8.021 Implement analyze() in the SDK delegating to the supervisor graph with zero inline analysis logic | **Status:** DONE | **DoD:** All analyze() tests pass with the supervisor mocked at the gatekeeper boundary
- [x] 8.022 Write failing tests for sdk.build_vault() returning the Obsidian vault artifact paths (hot.md, index.md, wiki/, log.md) from a mocked vault package | **Status:** DONE | **DoD:** build_vault() tests fail red asserting the missing method
- [x] 8.023 Implement build_vault() delegating to the vault package and returning a typed artifact-path result | **Status:** DONE | **DoD:** All build_vault() tests pass with vault writes mocked
- [x] 8.024 Write failing tests for sdk.run_loop() enforcing the Part C stop conditions and the MAX_LOOP_ITERATIONS 5-iteration hard cap from shared/constants.py | **Status:** DONE | **DoD:** run_loop() tests fail red asserting LoopResult fields not yet produced
- [x] 8.025 Implement run_loop() returning a LoopResult with per-iteration metric diffs and stop-reason field | **Status:** DONE | **DoD:** run_loop() tests pass and the iteration cap is sourced only from shared/constants.py MAX_LOOP_ITERATIONS
- [x] 8.026 Write failing tests for sdk.measure_tokens() returning a TokenReport (baseline vs Graphify-assisted totals, savings_pct, explanation_required flag when below 70%) | **Status:** DONE | **DoD:** measure_tokens() tests fail red asserting the missing method
- [x] 8.027 Implement measure_tokens() delegating to MetricsAgent for before/after token accounting | **Status:** DONE | **DoD:** All measure_tokens() tests pass with MetricsAgent mocked
- [x] 8.028 Write failing tests for sdk.validate_config() covering missing setup.json keys, rate_limits.json schema errors, and absent .env-example | **Status:** DONE | **DoD:** validate_config() tests fail red with the method absent
- [x] 8.029 Implement validate_config() raising ConfigError that names the offending key path | **Status:** DONE | **DoD:** All validate_config() tests pass including the ConfigError message-content assertion
- [x] 8.030 Write failing tests for the plugin registry (register_agent_plugin, duplicate-name rejection, lookup, config allowlist filtering) | **Status:** DONE | **DoD:** Registry tests fail red with plugins module absent
- [x] 8.031 Implement the plugin registry in src/archlens/sdk/plugins.py exposing the AgentPlugin extension point | **Status:** DONE | **DoD:** Registry tests pass and the file is ≤150 code lines
- [x] 8.032 Document the plugin authoring contract (interface, lifecycle, allowlist behavior) as a subsection of the SDK facade architecture section in docs/PLAN.md | **Status:** DONE | **DoD:** Subsection exists in docs/PLAN.md containing a worked AgentPlugin skeleton code block
- [x] 8.033 Write failing CLI tests for src/main.py asserting each subcommand (analyze, vault, loop, tokens) maps 1:1 to an SDK method and propagates exit codes | **Status:** DONE | **DoD:** CLI tests fail red against an empty src/main.py
- [x] 8.034 Implement the thin CLI in src/main.py with argparse parsing and ArchLensSDK delegation only (zero business logic) | **Status:** DONE | **DoD:** CLI tests pass and `uv run python src/main.py --help` exits 0
- [x] 8.035 Add a static guard test asserting src/main.py imports nothing from archlens.agents, graphops, vault, or metrics (SDK-only imports) | **Status:** DONE | **DoD:** Guard test passes and runs in the default `uv run pytest` suite
- [x] 8.036 Run a cross-file duplicate-function audit over src/ and extract any function appearing in 2+ files into a shared module | **Status:** DONE | **DoD:** `uv run pytest tests/test_dry_rules.py` passes reporting zero 2+ file duplicate functions
- [x] 8.037 Implement a shared error-handling wrapper (decorator/context manager) replacing identical try/except blocks appearing in 3+ files | **Status:** DONE | **DoD:** DRY audit shows the try/except pattern in ≤2 files and the full test suite stays green
- [x] 8.038 Extract a base class or mixin in shared/ for any method implemented in 3+ agent classes (e.g., evidence-citation formatting) | **Status:** DONE | **DoD:** Mixin exists, all affected agents inherit it, and the DRY audit test passes
- [x] 8.039 Create __init__.py with explicit __all__ for every package under src/archlens (sdk, gatekeeper, agents, graphops, vault, metrics, shared) | **Status:** DONE | **DoD:** `uv run python -c "import archlens"` exits 0 and ruff reports no F401 violations
- [x] 8.040 Expose __version__ in archlens/__init__.py sourced from shared/version.py (1.00) and matching pyproject.toml | **Status:** DONE | **DoD:** `uv run python -c "import archlens; print(archlens.__version__)"` prints 1.00 equal to the pyproject.toml version
- [x] 8.041 Convert all intra-package imports under src/archlens to relative imports | **Status:** DONE | **DoD:** grep for "from archlens" inside src/archlens returns 0 matches and `uv run ruff check src` exits 0
- [x] 8.042 Write failing concurrency tests exercising parallel agent execution through the SDK (ThreadPoolExecutor over the shared FIFO request queue and metrics accumulator) | **Status:** DONE | **DoD:** Concurrency tests fail red exposing unsynchronized shared state
- [x] 8.043 Implement thread-safety with threading.Lock guards on the FIFO overflow queue, metrics counters, and plugin registry | **Status:** DONE | **DoD:** All concurrency tests in tests/test_concurrency.py pass deterministically
- [x] 8.044 Verify thread-safety under a soak run repeating the concurrency suite 20 consecutive times | **Status:** DONE | **DoD:** 20/20 consecutive `uv run pytest tests/test_concurrency.py` runs exit 0 with no flakes
- [x] 8.045 Implement a repo-wide 150-line audit test counting code lines (blank/comment lines excluded) for every file in src/ and tests/ | **Status:** DONE | **DoD:** tests/test_line_budget.py exists and executes in the default `uv run pytest` suite
- [x] 8.046 Split every module flagged by the line-budget audit as exceeding 150 code lines (e.g., sdk.py → facade + _wiring.py) | **Status:** DONE | **DoD:** Line-budget test passes with 0 violations and the full test suite stays green
- [x] 8.047 Write Google-style API docstrings for every public SDK callable, DTO, and exception, backed by a docstring-coverage test | **Status:** DONE | **DoD:** Docstring-coverage test asserting non-empty __doc__ on 100% of public API members passes
- [x] 8.048 Write an SDK-level integration test driving analyze() → build_vault() → measure_tokens() end-to-end against a stub gatekeeper | **Status:** DONE | **DoD:** `uv run pytest tests/integration/test_sdk_flow.py` passes
- [x] 8.049 Write an SDK-level integration test for run_loop() covering stop-condition early exit and the 5-iteration hard cap | **Status:** DONE | **DoD:** `uv run pytest tests/integration/test_sdk_loop.py` passes
- [x] 8.050 Verify Phase 8 quality gates with the combined coverage and lint command | **Status:** DONE | **DoD:** `uv run pytest --cov=src/archlens --cov-branch --cov-fail-under=85 && uv run ruff check src tests` exits 0

## Phase 9 — API Gatekeeper & Rate Limiting

- [x] 9.001 Mechanism-team review of docs/PRD_api_gatekeeper.md for the gatekeeper mechanism (FR-xx/NFR-xx IDs, mermaid sequence diagram of request lifecycle, house-style header) | **Status:** DONE | **DoD:** Review findings recorded in the PRD_api_gatekeeper.md change log with 0 open findings; version header, at least 8 FR/NFR IDs, and one fenced mermaid sequenceDiagram block confirmed present
- [ ] 9.002 Obtain lecturer approval of docs/PRD_api_gatekeeper.md before gatekeeper implementation starts | **Status:** BLOCKED | **DoD:** PRD_api_gatekeeper.md header Status field reads "Approved" with lecturer sign-off date
- [x] 9.003 Record Phase 9 prompt-engineering entries in docs/PROMPT_BOOK.md as gatekeeper prompts are authored | **Status:** DONE | **DoD:** PROMPT_BOOK.md contains a "Phase 9 — Gatekeeper" section with one logged entry (prompt, model, outcome) per gatekeeper prompt used
- [x] 9.004 Define named constants for every rate_limits.json key per the PRD_api_gatekeeper.md §3.1 nested schema (version, requests_per_minute, requests_per_hour, concurrent_max, retry_after_seconds, max_retries, queue max_depth, queue backpressure_warn_ratio) in src/archlens/shared/constants.py | **Status:** DONE | **DoD:** constants.py defines all eight key-name constants and `uv run ruff check src/archlens/shared/constants.py` exits 0
- [x] 9.005 Author config/rate_limits.json per the PRD_api_gatekeeper.md §3.1 nested schema: top-level version="1.00" (validated at boot), rate_limits.services.default block with requests_per_minute=30, requests_per_hour=500, concurrent_max=5, retry_after_seconds=30, max_retries=3, and a queue block with max_depth and backpressure_warn_ratio | **Status:** DONE | **DoD:** config/rate_limits.json parses as valid JSON with the top-level version key, the rate_limits.services.default block holding the five specified values (requests_per_minute, requests_per_hour, concurrent_max, retry_after_seconds, max_retries), and queue.max_depth plus queue.backpressure_warn_ratio present
- [x] 9.006 Scaffold src/archlens/gatekeeper/ package with __init__.py exporting the Gatekeeper public API via __all__ | **Status:** DONE | **DoD:** `uv run python -c "import archlens.gatekeeper"` exits 0 and __init__.py lists Gatekeeper in __all__
- [x] 9.007 Configure tests/conftest.py fixtures for gatekeeper tests: fake_clock, rate_config_factory (temp rate_limits.json), and mock_anthropic client factory | **Status:** DONE | **DoD:** all three fixtures resolve in `uv run pytest --fixtures tests` output and are referenced by at least one collected test
- [x] 9.008 Write failing unit tests for the RateLimitConfig loader covering missing file, malformed JSON, and missing required keys | **Status:** DONE | **DoD:** `uv run pytest tests/test_rate_config.py` collects >= 4 tests and all fail (red) before loader implementation
- [x] 9.009 Implement RateLimitConfig loader in src/archlens/gatekeeper/rate_config.py reading config/rate_limits.json via cfg.get with zero hardcoded limit values | **Status:** DONE | **DoD:** `uv run pytest tests/test_rate_config.py` exits 0 and rate_config.py contains no numeric literals for 30/500/5/3 limits
- [x] 9.010 Write failing test asserting that a top-level version key mismatch (e.g. "0.99") raises ConfigVersionError on load | **Status:** DONE | **DoD:** test_rate_config.py::test_config_version_mismatch fails (red) against the current loader
- [x] 9.011 Implement boot-time version validation in the loader comparing the top-level version key against the supported version from shared/version.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_rate_config.py -k version` exits 0
- [x] 9.012 Write failing tests for the gatekeeper error taxonomy hierarchy (base class, subclass relationships, message/attribute contracts) | **Status:** DONE | **DoD:** `uv run pytest tests/test_gatekeeper_errors.py` collects >= 5 tests and all fail (red) before errors.py exists
- [x] 9.013 Implement error taxonomy in src/archlens/gatekeeper/errors.py (GatekeeperError base, ConfigVersionError, UpstreamAPIError, RetryExhaustedSignal, QueueSaturationSignal — internal signals never escape to callers) | **Status:** DONE | **DoD:** `uv run pytest tests/test_gatekeeper_errors.py` exits 0
- [x] 9.014 Implement injectable Clock protocol plus FakeClock test double in src/archlens/gatekeeper/clock.py with TDD tests written first | **Status:** DONE | **DoD:** `uv run pytest tests/test_clock.py` exits 0 and FakeClock.advance() drives time without any real sleep calls
- [x] 9.015 Write failing tests for the per-minute sliding window: request 30 within 60s dispatches, request 31 is deferred | **Status:** DONE | **DoD:** `uv run pytest tests/test_windows.py -k minute` shows red boundary tests for counts 30 vs 31
- [x] 9.016 Implement SlidingWindowCounter and the per-minute (30 req/min) window in src/archlens/gatekeeper/windows.py using the injectable Clock | **Status:** DONE | **DoD:** `uv run pytest tests/test_windows.py -k minute` exits 0
- [x] 9.017 Write failing tests for the per-hour window: request 500 within 3600s dispatches, request 501 is deferred | **Status:** DONE | **DoD:** `uv run pytest tests/test_windows.py -k hour` shows red boundary tests for counts 500 vs 501
- [x] 9.018 Implement the per-hour (500 req/hr) window reusing SlidingWindowCounter with config-driven capacity | **Status:** DONE | **DoD:** `uv run pytest tests/test_windows.py -k hour` exits 0
- [x] 9.019 Test window rollover for both windows: saturate, advance FakeClock past 61s / 3601s, assert full capacity restored | **Status:** DONE | **DoD:** `uv run pytest tests/test_windows.py -k rollover` exits 0 with zero real time.sleep calls in the test run
- [x] 9.020 Write failing tests for the concurrency cap: with 10 parallel submissions the 6th acquire waits and observed max in-flight equals 5 | **Status:** DONE | **DoD:** `uv run pytest tests/test_gatekeeper_concurrency.py` collects the in-flight instrumentation test and it fails (red) before implementation
- [x] 9.021 Implement concurrency limiter in src/archlens/gatekeeper/concurrency.py using threading.BoundedSemaphore sized from the concurrent_max config key | **Status:** DONE | **DoD:** `uv run pytest tests/test_gatekeeper_concurrency.py` exits 0 and semaphore size is read from RateLimitConfig.concurrent_max, not a literal
- [x] 9.022 Write failing tests for RetryPolicy: waits retry_after_seconds=30 via FakeClock between attempts and stops after max_retries=3 | **Status:** DONE | **DoD:** `uv run pytest tests/test_retry.py` collects >= 3 tests covering wait interval and attempt cap, all red
- [x] 9.023 Implement RetryPolicy in src/archlens/gatekeeper/retry.py with config-driven retry_after_seconds and max_retries | **Status:** DONE | **DoD:** `uv run pytest tests/test_retry.py` exits 0
- [x] 9.024 Implement retry-exhaustion handoff (test-first): after the 3rd failed retry the request is requeued to the overflow queue and the caller receives no exception | **Status:** DONE | **DoD:** test passes proving a 4-failure request lands back in the queue with zero exceptions propagated to the caller
- [x] 9.025 Write failing tests for the FIFO overflow queue: strict FIFO ordering, max depth read from the queue.max_depth config key, depth() gauge accuracy | **Status:** DONE | **DoD:** `uv run pytest tests/test_overflow_queue.py` collects >= 4 tests and all fail (red) before implementation
- [x] 9.026 Implement FIFO overflow queue in src/archlens/gatekeeper/overflow_queue.py (deque plus lock, config-driven max depth, depth gauge) | **Status:** DONE | **DoD:** `uv run pytest tests/test_overflow_queue.py` exits 0
- [x] 9.027 Write failing tests for backpressure: enqueue at max depth blocks the caller until space frees, with zero dropped requests and zero raised exceptions | **Status:** DONE | **DoD:** backpressure tests fail (red) and assert dropped_count == 0 and no exception escapes
- [x] 9.028 Implement blocking-enqueue backpressure in the overflow queue honoring the never-reject requirement | **Status:** DONE | **DoD:** `uv run pytest tests/test_overflow_queue.py -k backpressure` exits 0
- [x] 9.029 Write failing tests for the drain loop: queued requests dispatch in FIFO order as window and semaphore capacity frees | **Status:** DONE | **DoD:** `uv run pytest tests/test_drain.py` collects ordered-dispatch tests and all fail (red) before implementation
- [x] 9.030 Implement drain logic in src/archlens/gatekeeper/drain.py coordinating queue, windows, and semaphore via the injectable Clock | **Status:** DONE | **DoD:** `uv run pytest tests/test_drain.py` exits 0
- [x] 9.031 Test FIFO ordering across retry requeues: a requeued request re-enters at the tail and overall dispatch order stays deterministic | **Status:** DONE | **DoD:** `uv run pytest tests/test_drain.py -k requeue_order` exits 0
- [x] 9.032 Write failing tests for structured per-call logging: each call emits exactly one JSON record with ISO-8601 timestamp, model id, input_tokens, output_tokens, latency_ms, and queue_depth | **Status:** DONE | **DoD:** `uv run pytest tests/test_call_log.py` collects field-presence tests for all six fields and all fail (red)
- [x] 9.033 Implement structured call logger in src/archlens/gatekeeper/call_log.py with handlers loaded from config/logging_config.json | **Status:** DONE | **DoD:** `uv run pytest tests/test_call_log.py` exits 0 and no logging level/format literals appear outside config or constants.py
- [x] 9.034 Implement log redaction (test-first) so the API key value never appears in any emitted log record | **Status:** DONE | **DoD:** redaction test passes — scanning all captured log output for the fixture API key value yields 0 matches
- [x] 9.035 Write failing tests for token-ledger hooks: every completed call appends exactly one ledger entry with model, input_tokens, output_tokens, and usd_cost fields | **Status:** DONE | **DoD:** `uv run pytest tests/test_gatekeeper_ledger.py` collects >= 3 tests and all fail (red) before implementation
- [x] 9.036 Implement TokenLedger hooks in src/archlens/gatekeeper/ledger.py with a register_hook() callback consumable by MetricsAgent | **Status:** DONE | **DoD:** `uv run pytest tests/test_gatekeeper_ledger.py` exits 0 with a MetricsAgent stub successfully consuming entries
- [x] 9.037 Write failing tests for the Anthropic client wrapper using mocked HTTP: payload assembly, and 429/500/timeout responses mapped to UpstreamAPIError subclasses from the taxonomy | **Status:** DONE | **DoD:** `uv run pytest tests/test_anthropic_client.py` collects >= 5 tests and all fail (red) before implementation
- [x] 9.038 Implement AnthropicClient wrapper in src/archlens/gatekeeper/anthropic_client.py reading the API key exclusively via os.environ.get("ANTHROPIC_API_KEY") | **Status:** DONE | **DoD:** `uv run pytest tests/test_anthropic_client.py` exits 0 and no key literal or default key value exists in source
- [x] 9.039 Configure ANTHROPIC_API_KEY with a dummy value in .env-example and verify .env is git-ignored | **Status:** DONE | **DoD:** `git check-ignore .env` exits 0 and .env-example contains an ANTHROPIC_API_KEY line with a dummy value
- [x] 9.040 Write failing tests for mock mode: canned responses with deterministic token counts and zero network sockets opened during the test run | **Status:** DONE | **DoD:** `uv run pytest tests/test_mock_mode.py` collects determinism and no-network tests and all fail (red) before implementation
- [x] 9.041 Implement mock mode in src/archlens/gatekeeper/mock_client.py selected by a config flag so the full test suite runs offline | **Status:** DONE | **DoD:** `uv run pytest tests/test_mock_mode.py` exits 0 with network access disabled
- [x] 9.042 Write failing facade tests for Gatekeeper.execute(): immediate-capacity path returns a response, saturated path transparently queues then completes | **Status:** DONE | **DoD:** `uv run pytest tests/test_gatekeeper.py` collects both paths and all fail (red) before facade implementation
- [x] 9.043 Implement the Gatekeeper facade in src/archlens/gatekeeper/gatekeeper.py composing config, windows, semaphore, retry, queue, drain, call logger, and ledger behind a single execute() entry point | **Status:** DONE | **DoD:** `uv run pytest tests/test_gatekeeper.py` exits 0
- [x] 9.044 Test saturation behavior: submit 40 requests within one fake minute and assert 30 dispatch, 10 queue, 0 reject | **Status:** DONE | **DoD:** `uv run pytest tests/test_gatekeeper.py -k saturation` exits 0 with assertions dispatched==30, queued==10, rejected==0
- [x] 9.045 Test the never-reject/never-crash invariant with a stress test: 600 mock-mode requests against the 500/hr window all eventually complete | **Status:** DONE | **DoD:** stress test passes with completed==600, exceptions==0, dropped==0
- [x] 9.046 Test thread-safety under contention and harden locking: 50 threads x 20 calls each with shared window counters and queue | **Status:** DONE | **DoD:** `uv run pytest tests/test_gatekeeper_threads.py` exits 0 with ledger entry count exactly 1000 and consistent window counters
- [x] 9.047 Verify ALL external API calls route through the gatekeeper: no anthropic import or HTTP client usage outside src/archlens/gatekeeper/ | **Status:** DONE | **DoD:** grep for `anthropic|httpx|requests` imports across src/ excluding src/archlens/gatekeeper/ returns 0 matches
- [x] 9.048 Refactor every gatekeeper module to the 150-line cap by splitting (never compressing) oversized files | **Status:** DONE | **DoD:** line-count checker reports <= 150 non-blank non-comment lines for every file under src/archlens/gatekeeper/ and tests/ touched in this phase
- [x] 9.049 Verify Ruff compliance for the gatekeeper package and its tests | **Status:** DONE | **DoD:** `uv run ruff check src/archlens/gatekeeper tests` exits 0 with 0 violations
- [x] 9.050 Verify coverage of the gatekeeper package meets the project gate | **Status:** DONE | **DoD:** `uv run pytest --cov=archlens.gatekeeper --cov-branch --cov-fail-under=85` exits 0

## Phase 10 — Multi-Agent Orchestration (LangGraph)

- [x] 10.001 Review docs/PRD_agent_orchestration.md §3.2 and write failing unit tests in tests/test_agents_state.py asserting the AgentState schema exposes typed keys target_repo, graph_snapshot, findings, loop_iteration, stop_eval, token_ledger, approvals | **Status:** DONE | **DoD:** `uv run pytest tests/test_agents_state.py` collects at least 7 tests and exits red before implementation exists
- [x] 10.002 Implement AgentState TypedDict with per-key reducers (dict fields including token_ledger are last-write-wins; only findings and approvals append, per PRD_agent_orchestration §3.2) in src/archlens/agents/state.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_agents_state.py` passes with 0 failures
- [x] 10.003 Write failing unit tests in tests/test_handoff_contracts.py validating one handoff payload model per agent transition (supervisor to each agent and each agent back to supervisor) | **Status:** DONE | **DoD:** `uv run pytest tests/test_handoff_contracts.py` collects at least 8 contract tests and exits red
- [x] 10.004 Implement Pydantic handoff payload models in src/archlens/agents/contracts.py covering repo manifest, graph snapshot ref, finding batch, patch plan, QA report, and metrics delta | **Status:** DONE | **DoD:** `uv run pytest tests/test_handoff_contracts.py` passes with 0 failures
- [x] 10.005 Write failing unit tests in tests/test_evidence_contract.py for the BugHunter evidence-ladder output contract (levels OBSERVED, INFERRED, EXTRACTED, VALIDATED; citation triple relation, confidence, source_file; confidence bounds 0.55-0.95) | **Status:** DONE | **DoD:** `uv run pytest tests/test_evidence_contract.py` collects ladder-level and bounds cases and exits red
- [x] 10.006 Implement EvidenceFinding model in src/archlens/agents/evidence.py rejecting any claim missing its citation triple or carrying confidence outside 0.55-0.95 | **Status:** DONE | **DoD:** `uv run pytest tests/test_evidence_contract.py` passes with 0 failures
- [x] 10.007 Write failing unit tests in tests/test_supervisor.py covering every supervisor routing decision (initial to RepoAgent, repo validated to GraphAgent, graph ready to AnalystAgent, findings to BugHunter, validated bug to Refactor, patch applied to GraphAgent re-run, QA done to stop_eval, stop met to END) | **Status:** DONE | **DoD:** `uv run pytest tests/test_supervisor.py` collects at least 8 routing-branch tests and exits red
- [x] 10.008 Implement supervisor node in src/archlens/agents/supervisor.py that inspects state and returns the next-agent decision plus a reason string | **Status:** DONE | **DoD:** `uv run pytest tests/test_supervisor.py -k decision` passes with 0 failures
- [x] 10.009 Implement conditional routing function route_from_supervisor in src/archlens/agents/routing.py mapping every supervisor decision to a node name including END | **Status:** DONE | **DoD:** `uv run pytest tests/test_supervisor.py -k routing` passes for all branches including END
- [x] 10.010 Write failing smoke test in tests/test_graph_builder.py asserting the compiled StateGraph contains the supervisor plus 7 agent nodes with supervisor as entry point | **Status:** DONE | **DoD:** `uv run pytest tests/test_graph_builder.py` exits red before the builder exists
- [x] 10.011 Implement build_orchestration_graph() in src/archlens/agents/graph_builder.py wiring all nodes and conditional edges per the PLAN.md C4 component diagram | **Status:** DONE | **DoD:** `uv run pytest tests/test_graph_builder.py` passes with 0 failures
- [x] 10.012 Test graph topology export by asserting the compiled graph's draw_mermaid() output names all 8 nodes | **Status:** DONE | **DoD:** `uv run pytest tests/test_graph_builder.py -k mermaid` passes with 0 failures
- [x] 10.013 Write failing unit tests in tests/test_repo_agent_node.py asserting the RepoAgent node delegates clone/validate to the SDK only and writes a target_repo payload into state | **Status:** DONE | **DoD:** `uv run pytest tests/test_repo_agent_node.py` exits red before implementation exists
- [x] 10.014 Implement RepoAgent node function in src/archlens/agents/repo_agent.py calling only sdk.sdk entry points (no direct subprocess or git imports) | **Status:** DONE | **DoD:** `uv run pytest tests/test_repo_agent_node.py` passes with 0 failures
- [x] 10.015 Write failing unit tests in tests/test_graph_agent_node.py asserting the GraphAgent node triggers the Graphify pipeline via the SDK and stores graph_snapshot with graph.json path, node/edge counts, and REPORT.md path | **Status:** DONE | **DoD:** `uv run pytest tests/test_graph_agent_node.py` exits red before implementation exists
- [x] 10.016 Implement GraphAgent node function in src/archlens/agents/graph_agent.py populating state.graph_snapshot from the SDK Graphify result | **Status:** DONE | **DoD:** `uv run pytest tests/test_graph_agent_node.py` passes with 0 failures
- [x] 10.017 Write failing unit tests in tests/test_analyst_agent_node.py asserting the AnalystAgent node emits degree/betweenness centrality, community assignments, hub-vs-bottleneck labels, bridge list, and edge triage tags EXTRACTED/INFERRED/AMBIGUOUS | **Status:** DONE | **DoD:** `uv run pytest tests/test_analyst_agent_node.py` exits red before implementation exists
- [x] 10.018 Implement AnalystAgent node function in src/archlens/agents/analyst_agent.py appending typed analysis results into state.findings via the SDK | **Status:** DONE | **DoD:** `uv run pytest tests/test_analyst_agent_node.py` passes with 0 failures
- [x] 10.019 Write failing unit tests in tests/test_bughunter_agent_node.py asserting the BugHunterAgent node outputs only EvidenceFinding objects for SPOF, god nodes, bridges, and duplicate-logic candidates at similarity >= 0.91 | **Status:** DONE | **DoD:** `uv run pytest tests/test_bughunter_agent_node.py` exits red before implementation exists
- [x] 10.020 Implement BugHunterAgent node function in src/archlens/agents/bughunter_agent.py enforcing the evidence-ladder output contract on every emitted claim | **Status:** DONE | **DoD:** `uv run pytest tests/test_bughunter_agent_node.py` passes with 0 failures
- [x] 10.021 Write failing unit tests in tests/test_refactor_agent_node.py asserting the RefactorAgent node consumes the highest-priority VALIDATED finding and produces a patch plan payload before any write occurs | **Status:** DONE | **DoD:** `uv run pytest tests/test_refactor_agent_node.py` exits red before implementation exists
- [x] 10.022 Implement RefactorAgent node function in src/archlens/agents/refactor_agent.py applying fixes only through the SDK and only after guardrail tier clearance | **Status:** DONE | **DoD:** `uv run pytest tests/test_refactor_agent_node.py` passes with 0 failures
- [x] 10.023 Write failing unit tests in tests/test_qa_agent_node.py asserting the QAAgent node runs pytest, coverage, and ruff via the SDK and writes pass/fail plus metrics into the stop_eval inputs in state | **Status:** DONE | **DoD:** `uv run pytest tests/test_qa_agent_node.py` exits red before implementation exists
- [x] 10.024 Implement QAAgent node function in src/archlens/agents/qa_agent.py recording tests-green, coverage percentage, and ruff violation count into state | **Status:** DONE | **DoD:** `uv run pytest tests/test_qa_agent_node.py` passes with 0 failures
- [x] 10.025 Write failing unit tests in tests/test_metrics_agent_node.py asserting the MetricsAgent node appends before/after token entries and per-model cost rows to state.token_ledger | **Status:** DONE | **DoD:** `uv run pytest tests/test_metrics_agent_node.py` exits red before implementation exists
- [x] 10.026 Implement MetricsAgent node function in src/archlens/agents/metrics_agent.py computing token deltas and cost rows from gatekeeper usage records | **Status:** DONE | **DoD:** `uv run pytest tests/test_metrics_agent_node.py` passes with 0 failures
- [x] 10.027 Write failing unit tests in tests/test_stop_eval.py covering all five Part C stop conditions (bottleneck lost dependencies, fewer inter-community edges, no new isolated components, all tests green, ruff 0) plus the 5-iteration hard cap | **Status:** DONE | **DoD:** `uv run pytest tests/test_stop_eval.py` collects at least 6 condition tests and exits red
- [x] 10.028 Implement stop_eval node in src/archlens/agents/stop_eval.py computing diff metrics between consecutive graph snapshots and writing the verdict into state.stop_eval | **Status:** DONE | **DoD:** `uv run pytest tests/test_stop_eval.py -k conditions` passes with 0 failures
- [x] 10.029 Implement loop-iteration guard incrementing state.loop_iteration once per cycle and forcing routing to END when loop_iteration reaches 5 | **Status:** DONE | **DoD:** `uv run pytest tests/test_stop_eval.py -k hard_cap` passes asserting END at iteration 5
- [x] 10.030 Write failing tests in tests/test_checkpointing.py asserting a run interrupted after GraphAgent resumes at AnalystAgent with byte-identical restored state | **Status:** DONE | **DoD:** `uv run pytest tests/test_checkpointing.py` exits red before checkpointer wiring exists
- [x] 10.031 Configure the LangGraph SqliteSaver checkpointer with the database path read from config/setup.json via cfg.get (no hardcoded path) | **Status:** DONE | **DoD:** `uv run pytest tests/test_checkpointing.py -k saver` passes with 0 failures
- [x] 10.032 Implement resume entry point resume_run(thread_id) in src/archlens/agents/runner.py restoring the latest checkpoint and continuing from the next pending node | **Status:** DONE | **DoD:** `uv run pytest tests/test_checkpointing.py` passes with 0 failures including the interrupted-resume case
- [x] 10.033 Write failing unit tests in tests/test_guardrails.py for the three-tier action classifier (read-only executes automatically, reversible requires a registered undo path, irreversible requires human approval) | **Status:** DONE | **DoD:** `uv run pytest tests/test_guardrails.py` collects one test per tier and exits red
- [x] 10.034 Implement guardrail middleware in src/archlens/agents/guardrails.py wrapping every node action with tier classification before execution | **Status:** DONE | **DoD:** `uv run pytest tests/test_guardrails.py -k classifier` passes with 0 failures
- [x] 10.035 Implement undo-path registration for reversible actions creating a git branch snapshot before RefactorAgent writes with a rollback function | **Status:** DONE | **DoD:** test applying then rolling back a file edit restores the original content byte-identical
- [x] 10.036 Write failing tests in tests/test_approval_interrupt.py asserting the graph pauses on an irreversible action and records a pending entry in state.approvals | **Status:** DONE | **DoD:** `uv run pytest tests/test_approval_interrupt.py` exits red before interrupt wiring exists
- [x] 10.037 Implement human-in-the-loop approval interrupt using LangGraph interrupt() before any irreversible RefactorAgent write | **Status:** DONE | **DoD:** `uv run pytest tests/test_approval_interrupt.py -k pause` passes asserting the graph halts with a saved checkpoint
- [x] 10.038 Implement approval resume handler recording approver id, decision, and timestamp into state.approvals and blocking the write on rejection | **Status:** DONE | **DoD:** `uv run pytest tests/test_approval_interrupt.py` passes for both approve and reject paths
- [x] 10.039 Write failing tests in tests/test_node_retries.py asserting a transient gatekeeper error is retried and a permanent error surfaces after max_retries | **Status:** DONE | **DoD:** `uv run pytest tests/test_node_retries.py` exits red before the retry wrapper exists
- [x] 10.040 Implement per-node retry wrapper applying max_retries and retry_after backoff values read from config/rate_limits.json to every agent node | **Status:** DONE | **DoD:** `uv run pytest tests/test_node_retries.py` passes with 0 failures
- [x] 10.041 Write failing tests in tests/test_trace_log.py asserting one structured JSONL trace record per node transition containing node name, loop iteration, duration_ms, and token counts | **Status:** DONE | **DoD:** `uv run pytest tests/test_trace_log.py` exits red before the trace logger exists
- [x] 10.042 Implement run-trace logger in src/archlens/agents/trace.py honoring sinks and levels defined in config/logging_config.json | **Status:** DONE | **DoD:** `uv run pytest tests/test_trace_log.py` passes with 0 failures
- [x] 10.043 Verify trace completeness by asserting a full mocked run produces a trace file listing every visited node in execution order | **Status:** DONE | **DoD:** `uv run pytest tests/test_trace_log.py -k completeness` passes comparing trace sequence to the expected node order
- [x] 10.044 Write failing tests in tests/test_prompt_templates.py asserting each of the 7 agent prompt templates loads and renders with all required placeholders and no unresolved braces | **Status:** DONE | **DoD:** `uv run pytest tests/test_prompt_templates.py` collects 7 cases and exits red
- [x] 10.045 Author the 7 agent prompt template files under src/archlens/agents/prompts/ (one per agent, each with a Version: 1.00 header and declared variables) | **Status:** DONE | **DoD:** `uv run pytest tests/test_prompt_templates.py` passes with 0 failures
- [x] 10.046 Document all 7 agent prompt templates in docs/PROMPT_BOOK.md with prompt ID, version, intent, variables table, and iteration log entry | **Status:** IN_PROGRESS | **DoD:** docs/PROMPT_BOOK.md contains a Phase 10 section with 7 prompt entries each holding ID, version, and a variables table
- [x] 10.047 Configure a gatekeeper mock-mode fixture in tests/conftest.py returning deterministic canned LLM responses with all outbound sockets blocked | **Status:** DONE | **DoD:** orchestration tests using the fixture complete with 0 outbound HTTP calls as asserted by the blocked-socket fixture
- [x] 10.048 Write integration test for the supervisor-RepoAgent-GraphAgent handoff chain in gatekeeper mock mode validating the payload contract at each edge | **Status:** DONE | **DoD:** `uv run pytest tests/test_integration_handoff_repo_graph.py` passes with 0 failures
- [x] 10.049 Write integration test for the AnalystAgent-BugHunterAgent-RefactorAgent chain asserting evidence-ladder enforcement and guardrail tiering in mock mode | **Status:** DONE | **DoD:** `uv run pytest tests/test_integration_handoff_analysis.py` passes with 0 failures
- [x] 10.050 Write integration test for one full improvement-loop iteration (Refactor, Graphify re-run, QA, stop_eval diff) in mock mode | **Status:** DONE | **DoD:** `uv run pytest tests/test_integration_loop.py` passes asserting loop_iteration increments by exactly 1
- [x] 10.051 Create fixture repository under tests/fixtures/sample_repo/ containing a seeded god module and a duplicate-logic pair for pipeline tests | **Status:** DONE | **DoD:** Graphify run on the fixture yields at least 1 god-node finding and 1 duplicate-pair finding in the test assertion
- [x] 10.052 Write full-pipeline integration test executing the compiled graph end-to-end on the fixture repo in mock mode | **Status:** DONE | **DoD:** `uv run pytest tests/test_integration_full_pipeline.py` passes with stop_eval satisfied or hard cap reached within 5 iterations
- [x] 10.053 Verify statement plus branch coverage of src/archlens/agents/ meets the threshold | **Status:** DONE | **DoD:** `uv run pytest --cov=src/archlens/agents --cov-branch` reports coverage >= 85% with fail_under=85 enforced
- [x] 10.054 Verify Ruff reports zero violations across all Phase 10 modules and tests | **Status:** DONE | **DoD:** `uv run ruff check src/archlens/agents tests` exits with code 0
- [x] 10.055 Verify every Phase 10 source and test file stays within the 150-line limit excluding blank and comment lines | **Status:** DONE | **DoD:** the line-limit check script reports 0 files over 150 effective lines for src/archlens/agents/ and Phase 10 tests

## Phase 11 — Improvement Loop & Stop Conditions

- [x] 11.001 Mechanism-team review of docs/PRD_improvement_loop.md for the improvement-loop mechanism (loop controller, P1-P5 policy, stop conditions, house-style header) | **Status:** DONE | **DoD:** Review findings recorded in the PRD_improvement_loop.md change log with 0 open findings; "Version: 1.00" header and at least 8 FR-xx requirement IDs confirmed present.
- [ ] 11.002 Obtain lecturer approval of docs/PRD_improvement_loop.md before any Phase 11 development starts | **Status:** BLOCKED | **DoD:** Header status field changed from "Draft — awaiting lecturer approval" to "Approved" with approval date recorded.
- [x] 11.003 Add improvement_loop block to config/setup.json with keys max_iterations=5, priority_order, allowed_evidence_levels, branch_prefix | **Status:** DONE | **DoD:** `uv run python -c "import json; json.load(open('config/setup.json'))"` exits 0 and the block contains all four keys.
- [x] 11.004 Add loop enums to src/archlens/shared/constants.py (FixPriority P1-P5, EvidenceLevel, StopCondition, LoopVerdict) | **Status:** DONE | **DoD:** `uv run python -c "from archlens.shared.constants import FixPriority, EvidenceLevel, StopCondition"` exits 0.
- [x] 11.005 Write failing unit tests for LoopState model (iteration counter, applied-fix history, branch tracking, metrics snapshots) in tests/test_loop_state.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_loop_state.py` reports only failures (red) before implementation.
- [x] 11.006 Implement LoopState model in src/archlens/agents/loop_state.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_loop_state.py` passes with 0 failures.
- [x] 11.007 Write failing unit tests for FixPriorityPolicy ordering (SPOF=P1, bottleneck split=P2, >150-line module split=P3, validated duplicates=P4, PRD misalignment=P5) in tests/test_fix_policy.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_fix_policy.py` reports only failures (red) before implementation.
- [x] 11.008 Implement FixPriorityPolicy in src/archlens/agents/fix_policy.py reading priority_order from config via cfg.get (no hardcoded order) | **Status:** DONE | **DoD:** `uv run pytest tests/test_fix_policy.py` passes with 0 failures.
- [x] 11.009 Write failing unit tests for the evidence-level gate rejecting OBSERVED and INFERRED fix candidates in tests/test_fix_evidence_gate.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_fix_evidence_gate.py` reports only failures (red) before implementation.
- [x] 11.010 Implement evidence-level gate in src/archlens/agents/evidence_gate.py admitting only EXTRACTED and VALIDATED candidates with cited relation, confidence, and source_file | **Status:** DONE | **DoD:** `uv run pytest tests/test_fix_evidence_gate.py` passes with 0 failures.
- [x] 11.011 Write failing unit tests for the fix-candidate queue builder combining priority policy with evidence gate in tests/test_fix_queue.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_fix_queue.py` reports only failures (red) before implementation.
- [x] 11.012 Implement fix-candidate queue builder in src/archlens/agents/fix_queue.py returning gated candidates sorted P1 through P5 | **Status:** DONE | **DoD:** `uv run pytest tests/test_fix_queue.py` passes with 0 failures.
- [x] 11.013 Write failing unit tests for IterationBrancher creating one feature branch per iteration named fix/iter-<NN>-<slug> in tests/test_iteration_brancher.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_iteration_brancher.py` reports only failures (red) before implementation.
- [x] 11.014 Implement IterationBrancher in src/archlens/shared/gitops.py creating and checking out the per-iteration feature branch in the target repo | **Status:** DONE | **DoD:** `uv run pytest tests/test_iteration_brancher.py` passes and a temp-repo test shows branch fix/iter-01-* exists after one iteration.
- [x] 11.015 Write failing unit tests for the rollback helper reverting an iteration commit via `git revert` (never reset --hard) in tests/test_rollback.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_rollback.py` reports only failures (red) before implementation.
- [x] 11.016 Implement rollback helper in src/archlens/shared/gitops.py producing a revert commit that restores the pre-fix tree | **Status:** DONE | **DoD:** `uv run pytest tests/test_rollback.py` passes and the temp-repo working tree hash equals the pre-fix tree hash after rollback.
- [x] 11.017 Route all RefactorAgent LLM fix-generation calls through src/archlens/gatekeeper/gatekeeper.py with rate_limits.json applied | **Status:** DONE | **DoD:** `rg -l "anthropic|openai|httpx|requests" src/archlens/agents/` returns 0 matches (only gatekeeper imports an API client).
- [x] 11.018 Write failing unit tests for RefactorAgent oversized-module split (>150 code lines, blank/comment excluded) in tests/test_refactor_split.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_refactor_split.py` reports only failures (red) before implementation.
- [x] 11.019 Implement RefactorAgent.split_module producing modules each at or below 150 code lines with imports updated | **Status:** DONE | **DoD:** `uv run pytest tests/test_refactor_split.py` passes and each output fixture module measures <= 150 code lines.
- [x] 11.020 Write failing unit tests for RefactorAgent bottleneck-break fix (extract shared dependency into an interface module) in tests/test_refactor_bottleneck.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_refactor_bottleneck.py` reports only failures (red) before implementation.
- [x] 11.021 Implement RefactorAgent.break_bottleneck rewiring dependents from the bottleneck node to the extracted interface | **Status:** DONE | **DoD:** `uv run pytest tests/test_refactor_bottleneck.py` passes with 0 failures.
- [x] 11.022 Write failing unit tests for RefactorAgent duplicate-merge accepting only VALIDATED pairs with similarity >= 0.91 in tests/test_refactor_duplicates.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_refactor_duplicates.py` reports only failures (red) before implementation.
- [x] 11.023 Implement RefactorAgent.merge_duplicates relocating the validated duplicate into a shared module and updating both call sites | **Status:** DONE | **DoD:** `uv run pytest tests/test_refactor_duplicates.py` passes with 0 failures.
- [x] 11.024 Write failing unit tests for RefactorAgent SPOF mitigation (introduce abstraction/redundancy so the single-point node loses exclusive dependents) in tests/test_refactor_spof.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_refactor_spof.py` reports only failures (red) before implementation.
- [x] 11.025 Implement RefactorAgent.mitigate_spof in src/archlens/agents/refactor_fixes.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_refactor_spof.py` passes with 0 failures.
- [x] 11.026 Write failing unit tests for TestGate mapping pytest exit codes of the target repo to PASS/FAIL verdicts in tests/test_test_gate.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_test_gate.py` reports only failures (red) before implementation.
- [x] 11.027 Implement TestGate in src/archlens/agents/test_gate.py executing the target repo's full unit-test suite via `uv run pytest` after every applied change | **Status:** DONE | **DoD:** `uv run pytest tests/test_test_gate.py` passes with 0 failures.
- [x] 11.028 Wire the red-gate-to-rollback transition so any failing target test triggers git revert of the iteration commit | **Status:** DONE | **DoD:** Integration test planting one failing target test shows a revert commit in `git log` and a green suite afterwards.
- [x] 11.029 Write failing unit tests for the per-iteration Graphify re-run wrapper (detect -> extract -> build -> cluster -> export) in tests/test_graph_rerun.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_graph_rerun.py` reports only failures (red) before implementation.
- [x] 11.030 Implement Graphify re-run step in src/archlens/graphops/rerun.py regenerating graph.json, graph.html, and REPORT.md each iteration | **Status:** DONE | **DoD:** `uv run pytest tests/test_graph_rerun.py` passes and a fresh graph.json exists in the iteration output dir.
- [x] 11.031 Write failing unit tests for degree-delta computation between two graph.json snapshots in tests/test_diff_degree.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_diff_degree.py` reports only failures (red) before implementation.
- [x] 11.032 Implement per-node degree-delta computation in src/archlens/metrics/graph_diff.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_diff_degree.py` passes with 0 failures.
- [x] 11.033 Write failing unit tests for betweenness-centrality delta computation in tests/test_diff_betweenness.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_diff_betweenness.py` reports only failures (red) before implementation.
- [x] 11.034 Implement betweenness-centrality delta computation in src/archlens/metrics/graph_diff.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_diff_betweenness.py` passes with 0 failures.
- [x] 11.035 Write failing unit tests for inter-community edge-count diff (modularity-improvement signal) in tests/test_diff_communities.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_diff_communities.py` reports only failures (red) before implementation.
- [x] 11.036 Implement inter-community edge-count diff in src/archlens/metrics/graph_diff.py flagging improvement only when the count strictly decreases | **Status:** DONE | **DoD:** `uv run pytest tests/test_diff_communities.py` passes with 0 failures.
- [x] 11.037 Write failing unit tests for isolated-component detection on the post-fix graph in tests/test_diff_isolation.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_diff_isolation.py` reports only failures (red) before implementation.
- [x] 11.038 Implement isolated-component diff check in src/archlens/metrics/graph_diff.py failing the iteration when new isolated components appear | **Status:** DONE | **DoD:** `uv run pytest tests/test_diff_isolation.py` passes with 0 failures.
- [x] 11.039 Write failing unit tests for the moved-vs-removed load detector (bottleneck dependencies relocated to a neighbor node vs truly eliminated) in tests/test_load_shift.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_load_shift.py` reports only failures (red) before implementation.
- [x] 11.040 Implement moved-vs-removed load detector in src/archlens/metrics/load_shift.py rejecting iterations where load merely migrated to an adjacent node | **Status:** DONE | **DoD:** `uv run pytest tests/test_load_shift.py` passes with 0 failures.
- [x] 11.041 Write failing unit tests for StopConditionEvaluator covering all 5 conditions (dependencies lost not moved, modularity improved, no new isolated components, tests green, ruff 0) plus the 5-iteration hard cap in tests/test_stop_conditions.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_stop_conditions.py` reports only failures (red) before implementation.
- [x] 11.042 Implement StopConditionEvaluator in src/archlens/agents/stop_evaluator.py returning CONTINUE/STOP with the per-condition verdict vector | **Status:** DONE | **DoD:** `uv run pytest tests/test_stop_conditions.py` passes with 0 failures.
- [x] 11.043 Write failing unit tests for the iteration report generator (before/after metrics table plus decision-log entries) in tests/test_iteration_report.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_iteration_report.py` reports only failures (red) before implementation.
- [x] 11.044 Implement iteration report generator in src/archlens/metrics/iteration_report.py writing reports/iteration_<NN>.md with before/after table and decision log | **Status:** DONE | **DoD:** `uv run pytest tests/test_iteration_report.py` passes and a sample report contains both a Markdown table and a "Decision Log" section.
- [x] 11.045 Write failing integration test for the LoopController LangGraph subgraph (select fix -> branch -> apply -> test gate -> re-run Graphify -> diff -> evaluate stop) in tests/test_loop_controller.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_loop_controller.py` reports only failures (red) before implementation.
- [x] 11.046 Implement LoopController in src/archlens/agents/loop_controller.py and expose run_improvement_loop() through src/archlens/sdk/sdk.py only | **Status:** DONE | **DoD:** `uv run pytest tests/test_loop_controller.py` passes and `rg -l "loop_controller" src/main.py` returns 0 matches.
- [x] 11.047 Add thin --loop CLI flag to src/main.py delegating directly to sdk.run_improvement_loop with zero business logic | **Status:** DONE | **DoD:** `uv run python src/main.py --help` lists --loop and src/main.py contains no import from archlens.agents.
- [x] 11.048 Build convergence fixture repo under tests/fixtures/loop_target/ with a planted SPOF node, one >150-line module, and one duplicate function pair (similarity >= 0.91) plus its own green pytest suite | **Status:** DONE | **DoD:** Fixture contains all three planted defects and `uv run pytest tests/fixtures/loop_target/tests` passes standalone.
- [x] 11.049 Write end-to-end convergence test asserting the loop on the fixture repo terminates within 5 iterations with all 5 stop conditions satisfied and a report per iteration | **Status:** DONE | **DoD:** `uv run pytest -k convergence` passes with iteration count <= 5 asserted.
- [x] 11.050 Verify Phase 11 quality gates across all new modules: coverage >= 85%, Ruff 0 violations, every new file at or below 150 code lines | **Status:** DONE | **DoD:** `uv run pytest --cov` (fail_under=85) and `uv run ruff check src tests` both exit 0.

## Phase 12 — Token Measurement & Cost Analysis

- [x] 12.001 Mechanism-team review of docs/PRD_token_metrics.md (ledger model, savings formula, >= 70% target, amortization, FR-xx/NFR-xx ids, house-style header) | **Status:** DONE | **DoD:** Review findings recorded in the PRD_token_metrics.md change log with 0 open findings; version header, at least 5 FR ids, and the savings formula confirmed verbatim
- [ ] 12.002 Obtain lecturer approval for docs/PRD_token_metrics.md before Phase 12 implementation starts | **Status:** BLOCKED | **DoD:** Doc header status changed from "Draft — awaiting lecturer approval" to "Approved" with approval date
- [x] 12.003 Create synthetic-ledger fixture factory in tests/conftest.py generating configurable TokenLedger entries (agent, model, protocol, in/out tokens) | **Status:** DONE | **DoD:** uv run pytest tests/test_fixture_smoke.py passes asserting a 10-entry synthetic ledger is produced
- [x] 12.004 Write failing unit tests for TokenLedgerEntry and TokenLedger (field validation, append, totals, filter by agent/model/protocol/question_id) in tests/test_ledger.py | **Status:** DONE | **DoD:** uv run pytest tests/test_ledger.py exits non-zero (red) before implementation
- [x] 12.005 Implement TokenLedgerEntry dataclass and TokenLedger aggregate in src/archlens/metrics/ledger_model.py and src/archlens/metrics/ledger.py (each <= 150 code lines) | **Status:** DONE | **DoD:** uv run pytest tests/test_ledger.py passes green
- [x] 12.006 Write failing tests for ledger JSONL persistence round-trip (save, load, identical entries, corrupt-line error) in tests/test_ledger_io.py | **Status:** DONE | **DoD:** uv run pytest tests/test_ledger_io.py exits non-zero (red) before implementation
- [x] 12.007 Implement JSONL save/load in src/archlens/metrics/ledger_io.py with output path read via cfg.get from config/setup.json (no hardcoded paths) | **Status:** DONE | **DoD:** uv run pytest tests/test_ledger_io.py passes green
- [x] 12.008 Write failing tests for the gatekeeper usage-recording hook using mocked LLM responses (every call appends one ledger entry with agent, model, input_tokens, output_tokens) in tests/test_gatekeeper_ledger.py | **Status:** DONE | **DoD:** uv run pytest tests/test_gatekeeper_ledger.py exits non-zero (red) before implementation
- [x] 12.009 Implement record_usage hook inside src/archlens/gatekeeper/gatekeeper.py so every outbound LLM call writes a TokenLedgerEntry before returning the response | **Status:** DONE | **DoD:** uv run pytest tests/test_gatekeeper_ledger.py passes green
- [x] 12.010 Verify zero ledger bypass: static check that the LLM client library is imported only inside the gatekeeper module | **Status:** DONE | **DoD:** rg of the LLM client import across src/ returns matches only in src/archlens/gatekeeper/gatekeeper.py
- [x] 12.011 Configure per-model pricing table (USD per MTok input and USD per MTok output) in config/setup.json under a "pricing" key | **Status:** DONE | **DoD:** config/setup.json parses as JSON and contains >= 2 models each with input_per_mtok and output_per_mtok numeric fields
- [x] 12.012 Write failing tests for the cost calculator (single-entry cost, ledger total cost, unknown-model raises ConfigError) in tests/test_cost.py | **Status:** DONE | **DoD:** uv run pytest tests/test_cost.py exits non-zero (red) before implementation
- [x] 12.013 Implement cost calculator in src/archlens/metrics/cost.py reading the pricing table from config (no hardcoded prices) | **Status:** DONE | **DoD:** uv run pytest tests/test_cost.py passes green
- [x] 12.014 Write config/questions.json containing the 10 standard architecture questions (ids Q01-Q10, question text, expected evidence type) used by both protocols | **Status:** DONE | **DoD:** config/questions.json parses as JSON with exactly 10 entries having unique Q-ids and non-empty text
- [x] 12.015 Write failing tests for the questions loader (exactly 10 unique ids, non-empty text, missing-file error) in tests/test_questions.py | **Status:** DONE | **DoD:** uv run pytest tests/test_questions.py exits non-zero (red) before implementation
- [x] 12.016 Implement questions loader in src/archlens/metrics/questions.py exposed through the SDK | **Status:** DONE | **DoD:** uv run pytest tests/test_questions.py passes green
- [x] 12.017 Write failing tests for the baseline naive context builder using a fixture mini-repo (all .py files concatenated, deterministic order, token estimate returned) in tests/test_baseline_context.py | **Status:** DONE | **DoD:** uv run pytest tests/test_baseline_context.py exits non-zero (red) before implementation
- [x] 12.018 Implement baseline context builder (naive full-context stuffing of all target sources) in src/archlens/metrics/baseline_context.py | **Status:** DONE | **DoD:** uv run pytest tests/test_baseline_context.py passes green
- [x] 12.019 Write failing tests for BaselineRunner answering all 10 questions through the gatekeeper with mocked LLM and tagging entries protocol="baseline" in tests/test_baseline_runner.py | **Status:** DONE | **DoD:** uv run pytest tests/test_baseline_runner.py exits non-zero (red) before implementation
- [x] 12.020 Implement BaselineRunner in src/archlens/metrics/baseline_runner.py invoked only via the SDK entry point | **Status:** DONE | **DoD:** uv run pytest tests/test_baseline_runner.py passes green
- [x] 12.021 Run the baseline protocol on the target repo for all 10 questions via uv run and persist the ledger | **Status:** DONE | **DoD:** metrics/out/baseline_ledger.jsonl exists with >= 10 entries tagged protocol="baseline" (one per Q-id)
- [x] 12.022 Write failing tests for the assisted retriever (reads vault index.md hub first, then selects 2-3 wiki pages plus the question-relevant subgraph from graph.json) in tests/test_assisted_retriever.py | **Status:** DONE | **DoD:** uv run pytest tests/test_assisted_retriever.py exits non-zero (red) before implementation
- [x] 12.023 Implement assisted retriever in src/archlens/metrics/assisted_retriever.py enforcing a hard cap of 3 wiki pages per question | **Status:** DONE | **DoD:** uv run pytest tests/test_assisted_retriever.py passes green
- [x] 12.024 Write failing tests for AssistedRunner asserting assembled context contains no raw full-source dump and entries are tagged protocol="assisted" in tests/test_assisted_runner.py | **Status:** DONE | **DoD:** uv run pytest tests/test_assisted_runner.py exits non-zero (red) before implementation
- [x] 12.025 Implement AssistedRunner in src/archlens/metrics/assisted_runner.py invoked only via the SDK entry point | **Status:** DONE | **DoD:** uv run pytest tests/test_assisted_runner.py passes green
- [x] 12.026 Run the assisted protocol on the target repo for all 10 questions via uv run and persist the ledger | **Status:** DONE | **DoD:** metrics/out/assisted_ledger.jsonl exists with >= 10 entries tagged protocol="assisted" (one per Q-id)
- [x] 12.027 Write failing tests for per-agent and per-model accounting aggregation (token and dollar breakdown rows plus grand totals) using the synthetic-ledger fixture in tests/test_accounting.py | **Status:** DONE | **DoD:** uv run pytest tests/test_accounting.py exits non-zero (red) before implementation
- [x] 12.028 Implement accounting aggregator in src/archlens/metrics/accounting.py producing per-agent and per-model token/cost breakdowns | **Status:** DONE | **DoD:** uv run pytest tests/test_accounting.py passes green
- [x] 12.029 Write failing tests for the savings formula savings_pct = (1 - assisted_tokens / baseline_tokens) * 100 including zero-baseline guard and target_met flag at >= 70 in tests/test_savings.py | **Status:** DONE | **DoD:** uv run pytest tests/test_savings.py exits non-zero (red) before implementation
- [x] 12.030 Implement savings calculator in src/archlens/metrics/savings.py returning savings_pct and target_met against the configurable 70% threshold | **Status:** DONE | **DoD:** uv run pytest tests/test_savings.py passes green
- [x] 12.031 Write failing tests for graph-build amortization and break_even_queries = ceil(graph_build_tokens / per_query_token_savings) including infinite/None result when savings <= 0 in tests/test_amortization.py | **Status:** DONE | **DoD:** uv run pytest tests/test_amortization.py exits non-zero (red) before implementation
- [x] 12.032 Implement amortization calculator in src/archlens/metrics/amortization.py charging the one-time Graphify build cost against per-query savings | **Status:** DONE | **DoD:** uv run pytest tests/test_amortization.py passes green
- [x] 12.033 Write failing tests for the Table-4-style cost table generator (markdown columns: model, input tokens, output tokens, $/MTok in, $/MTok out, total $; totals row last) in tests/test_cost_tables.py | **Status:** DONE | **DoD:** uv run pytest tests/test_cost_tables.py exits non-zero (red) before implementation
- [x] 12.034 Implement cost table generator in src/archlens/metrics/cost_tables.py writing docs/metrics/COST_TABLES.md from real or synthetic ledgers | **Status:** DONE | **DoD:** uv run pytest tests/test_cost_tables.py passes green
- [x] 12.035 Write failing tests for budget alerts (warning emitted at configurable percent of token budget, call still completes, never raises) in tests/test_budget_alerts.py | **Status:** DONE | **DoD:** uv run pytest tests/test_budget_alerts.py exits non-zero (red) before implementation
- [x] 12.036 Implement budget alerts in src/archlens/gatekeeper/gatekeeper.py reading budget and alert threshold from config/rate_limits.json and logging a structured warning without crashing or rejecting | **Status:** DONE | **DoD:** uv run pytest tests/test_budget_alerts.py passes green
- [x] 12.037 Write docs/metrics/SAVINGS_EXPLANATION_TEMPLATE.md covering the Part A reality check (initial graph-scan cost amortization) with placeholders {{savings_pct}}, {{graph_build_tokens}}, {{break_even_queries}} | **Status:** DONE | **DoD:** Template file exists and contains all three named placeholders
- [x] 12.038 Write failing test for the explanation flow: a synthetic ledger pair yielding savings < 70% must produce a filled explanation file with no remaining placeholders in tests/test_explanation.py | **Status:** DONE | **DoD:** uv run pytest tests/test_explanation.py exits non-zero (red) before implementation
- [x] 12.039 Implement explanation flow in src/archlens/metrics/explanation.py rendering the template to docs/metrics/SAVINGS_EXPLANATION.md whenever target_met is false | **Status:** DONE | **DoD:** uv run pytest tests/test_explanation.py passes green
- [x] 12.040 Write failing tests for the token_metrics.json export schema (top-level keys: savings, per_agent, per_model, cost_totals, amortization, target_met) in tests/test_metrics_export.py | **Status:** DONE | **DoD:** uv run pytest tests/test_metrics_export.py exits non-zero (red) before implementation
- [x] 12.041 Implement metrics JSON export as an SDK method writing metrics/out/token_metrics.json with the full schema | **Status:** DONE | **DoD:** uv run pytest tests/test_metrics_export.py passes green
- [x] 12.042 Write failing tests for chart generation with synthetic ledgers (bar: baseline vs assisted tokens per question; waterfall: cumulative savings) asserting both PNG files are created non-empty in tests/test_charts.py | **Status:** DONE | **DoD:** uv run pytest tests/test_charts.py exits non-zero (red) before implementation
- [x] 12.043 Implement chart generation in src/archlens/metrics/charts.py saving baseline_vs_assisted_bar.png and savings_waterfall.png for the results notebook | **Status:** DONE | **DoD:** uv run pytest tests/test_charts.py passes green
- [x] 12.044 Run the end-to-end Phase 12 pipeline via uv run on the real baseline and assisted ledgers and record the outcome | **Status:** DONE | **DoD:** metrics/out/token_metrics.json exists with non-null savings_pct and either target_met=true or a filled docs/metrics/SAVINGS_EXPLANATION.md present
- [x] 12.045 Verify Phase 12 quality gates across all metrics modules and tests | **Status:** DONE | **DoD:** uv run pytest --cov reports >= 85% coverage, uv run ruff check reports 0 violations, and every Phase 12 file is <= 150 code lines

## Phase 13 — Testing & Quality Gates

- [x] 13.001 Configure [tool.pytest.ini_options] in pyproject.toml with testpaths=["tests"] and strict addopts | **Status:** DONE | **DoD:** `uv run pytest --collect-only` exits 0 reading configuration from pyproject.toml only.
- [x] 13.002 Configure statement+branch coverage in pyproject.toml ([tool.coverage.run] branch=true, source=["src/archlens"]) | **Status:** DONE | **DoD:** `uv run pytest --cov` report shows a Branch column for src/archlens modules.
- [x] 13.003 Set fail_under=85 under [tool.coverage.report] in pyproject.toml | **Status:** DONE | **DoD:** a run with total coverage below 85% makes `uv run pytest --cov` exit non-zero.
- [x] 13.004 Configure coverage omit list excluding src/main.py, tests/*, and GUI modules in pyproject.toml | **Status:** DONE | **DoD:** coverage report output contains no row for src/main.py or any tests/ file.
- [x] 13.005 Configure Ruff in pyproject.toml with line-length=100, target-version="py310", select=["E","F","W","I","N","UP","B","C4","SIM"], ignore=["E501"] | **Status:** DONE | **DoD:** pyproject.toml contains exactly these keys and `uv run ruff check .` runs using them.
- [x] 13.006 Write quality-gate test tests/test_ruff_gate.py running `uv run ruff check src tests` via subprocess and asserting exit code 0 | **Status:** DONE | **DoD:** gate test fails when a seeded violation file is present and passes after its removal.
- [x] 13.007 Create tests/conftest.py with a fixture_repo fixture building a temporary mini Python repo (3 packages, one import cycle, one >150-line module) | **Status:** DONE | **DoD:** fixture yields an existing tmp_path tree matching the layout documented in the conftest docstring.
- [x] 13.008 Add fixture_graph_json fixture to conftest.py returning a schema-valid Graphify graph.json sample with known nodes, edges, and communities | **Status:** DONE | **DoD:** fixture output validates against the graph schema helper in a passing sanity test.
- [x] 13.009 Add mock_gatekeeper fixture to conftest.py stubbing all Gatekeeper methods with call recording | **Status:** DONE | **DoD:** a sample test asserts call counts on the stub and no real API client object is constructed.
- [x] 13.010 Add mock_llm_responses fixture to conftest.py serving canned LLM completions keyed by prompt hash | **Status:** DONE | **DoD:** two identical prompts return identical canned payloads with zero network activity.
- [x] 13.011 Add mock_git fixture monkeypatching clone/checkout/status operations onto tmp_path repositories | **Status:** DONE | **DoD:** RepoAgent tests pass in a fully offline run with no remote contacted.
- [x] 13.012 Add mock_graphify fixture returning canned detect->extract->build->cluster->export outputs (graph.json, graph.html, REPORT.md) | **Status:** DONE | **DoD:** fixture writes all three artifacts into tmp_path and a sample test reads them back.
- [x] 13.013 Add fake_vault_fs fixture providing a tmp_path-backed Obsidian vault layout (hot.md, index.md, wiki/, log.md) | **Status:** DONE | **DoD:** all four fixture-created paths exist and are writable in a passing sample test.
- [x] 13.014 Implement autouse no-network fixture in conftest.py blocking socket.connect during every test | **Status:** DONE | **DoD:** a sentinel test performing a real HTTP request fails with the blocked-socket error.
- [x] 13.015 Write meta-test scanning tests/ for forbidden real-service imports (anthropic, openai, requests, httpx clients) | **Status:** DONE | **DoD:** meta-test passes on tests/ and fails when a seeded forbidden import is added.
- [x] 13.016 Write failing unit tests (TDD red) for shared/version.py and shared/constants.py before their implementation | **Status:** DONE | **DoD:** git history shows the failing-test commit preceding the implementation commit and tests now pass.
- [x] 13.017 Write unit tests for every public function in shared/ helpers (cfg.get config loader paths, error wrappers) | **Status:** DONE | **DoD:** coverage report shows 100% statements for the shared/ package.
- [x] 13.018 Write failing unit tests (TDD red) for gatekeeper rate-limit windows (30 req/min, 500 req/hr, 5 concurrent) using a mocked clock | **Status:** DONE | **DoD:** boundary values 30/31, 500/501, and 5/6 are each asserted and the red commit precedes the limiter implementation.
- [x] 13.019 Write unit tests for the gatekeeper FIFO overflow queue (config-driven max depth, queue-never-reject, never-crash) | **Status:** DONE | **DoD:** test proves request 31 within a minute is queued not rejected and dequeues in FIFO order.
- [x] 13.020 Write unit tests for gatekeeper retry policy (retry_after=30s, max_retries=3) with a fake timer | **Status:** DONE | **DoD:** test asserts exactly 3 retries spaced 30 simulated seconds apart on a 429, then a typed error is raised.
- [x] 13.021 Write AST-based test asserting every external API call site routes through gatekeeper/gatekeeper.py | **Status:** DONE | **DoD:** test finds zero direct API client instantiations outside gatekeeper.py and fails on a seeded violation.
- [x] 13.022 Write failing unit tests (TDD red) for each public sdk.py entry point signature and result contract | **Status:** DONE | **DoD:** one red test per SDK entry point exists before implementation and all later pass.
- [x] 13.023 Write unit tests covering every public SDK function happy path plus one failure path using the mock fixtures | **Status:** DONE | **DoD:** coverage for sdk/sdk.py is at least 95% statements and branches.
- [x] 13.024 Implement path-coverage supplement: parametrized tests enumerating distinct branch-combination paths through gatekeeper retry/queue logic and SDK dispatch | **Status:** DONE | **DoD:** every path ID listed in the test module docstring has a parametrized case and all cases pass.
- [x] 13.025 Write meta-test asserting every public function in src/archlens is referenced by at least one test | **Status:** DONE | **DoD:** meta-test enumerates public callables via inspection and passes with zero untested names.
- [x] 13.026 Write AST-based test asserting src/main.py contains zero business logic (argument parsing and SDK delegation only) | **Status:** DONE | **DoD:** test fails when a non-SDK call or computation is seeded into src/main.py and passes on the real file.
- [x] 13.027 Write failing unit tests (TDD red) for RepoAgent clone and validate flows using mock_git | **Status:** DONE | **DoD:** red commit precedes the RepoAgent implementation commit and all RepoAgent tests pass.
- [x] 13.028 Write unit tests for RepoAgent error paths (invalid URL, missing setup.json target, dirty workspace) | **Status:** DONE | **DoD:** each error path asserts a typed exception and RepoAgent branch coverage is at least 90%.
- [x] 13.029 Write failing unit tests (TDD red) for GraphAgent pipeline ordering detect->extract->build->cluster->export using mock_graphify | **Status:** DONE | **DoD:** test asserts the exact call order on the mock and the red commit precedes implementation.
- [x] 13.030 Write unit tests for GraphAgent Obsidian vault generation (hot.md, index.md, wiki/ pages, log.md) into fake_vault_fs | **Status:** DONE | **DoD:** all four artifacts exist with the expected non-empty sections after the test run.
- [x] 13.031 Write failing unit tests (TDD red) for AnalystAgent degree and betweenness centrality against hand-computed values on fixture_graph_json | **Status:** DONE | **DoD:** expected values in assertions match the hand computation documented in the test docstring.
- [x] 13.032 Write unit tests for AnalystAgent community detection and hub-vs-bottleneck classification on crafted ground-truth graphs | **Status:** DONE | **DoD:** classifier output matches the planted hub, bottleneck, and plain-node labels exactly.
- [x] 13.033 Write unit tests for AnalystAgent bridge detection and edge triage with confidence bounds | **Status:** DONE | **DoD:** every triaged edge is labeled EXTRACTED, INFERRED, or AMBIGUOUS with confidence asserted within [0.55, 0.95].
- [x] 13.034 Write failing unit tests (TDD red) for the BugHunterAgent evidence ladder ordering OBSERVED->INFERRED->EXTRACTED->VALIDATED | **Status:** DONE | **DoD:** test rejects any finding that skips a ladder stage and the red commit precedes implementation.
- [x] 13.035 Write unit tests asserting every BugHunterAgent claim carries a relation->confidence->source_file citation | **Status:** DONE | **DoD:** schema test fails on a finding missing any of the three citation fields and passes on valid findings.
- [x] 13.036 Write unit tests for the duplicate-logic similarity triage threshold | **Status:** DONE | **DoD:** similarity 0.91 is flagged and 0.909 is not, both asserted in the same test module.
- [x] 13.037 Write unit tests for Single Point of Failure and god-node detection rules on crafted fixture graphs | **Status:** DONE | **DoD:** detector flags the planted SPOF and god node and produces zero false positives on the fixture.
- [x] 13.038 Write failing unit tests (TDD red) for the RefactorAgent module-split planner on a >150-line fixture module | **Status:** DONE | **DoD:** planner output splits the oversized fixture module and the red commit precedes implementation.
- [x] 13.039 Write unit tests for improvement-loop stop conditions (bottleneck lost dependencies, improved modularity, no new isolated components, tests green, ruff 0, 5-iteration hard cap) | **Status:** DONE | **DoD:** each of the six conditions has a dedicated passing assertion including the iteration cap.
- [x] 13.040 Write unit tests for QAAgent wrappers around pytest, coverage, and ruff using a subprocess stub | **Status:** DONE | **DoD:** QAAgent returns structured pass/fail results from stubbed exit codes without spawning real tool runs.
- [x] 13.041 Write unit tests for MetricsAgent token accounting and per-model cost tables | **Status:** DONE | **DoD:** known token inputs produce the exact expected dollar figures in the rendered cost table.
- [x] 13.042 Write unit tests for LangGraph supervisor StateGraph wiring with all agent nodes mocked | **Status:** DONE | **DoD:** test asserts the supervisor visits nodes in the expected sequence for a happy-path run.
- [x] 13.043 Write unit tests for graphops metric diffing (inter-community edge deltas, isolated-component detection) on before/after fixture graphs | **Status:** DONE | **DoD:** diff output equals the hand-computed delta values for both fixture pairs.
- [x] 13.044 Write unit tests for loading setup.json, rate_limits.json, and logging_config.json including missing-key error paths | **Status:** DONE | **DoD:** each missing required key raises a typed configuration error asserted by a dedicated test.
- [x] 13.045 Write unit tests verifying .env is git-ignored and .env-example exists containing dummy values only | **Status:** DONE | **DoD:** test parses .gitignore for the .env entry and asserts no real-looking secret patterns in .env-example.
- [x] 13.046 Implement the 150-line-cap checker as tests/test_line_cap.py counting non-blank, non-comment lines for every code file including tests | **Status:** DONE | **DoD:** `uv run pytest tests/test_line_cap.py` passes across all of src/ and tests/.
- [x] 13.047 Verify the line-cap checker fails on a deliberately oversized temporary file (negative control) | **Status:** DONE | **DoD:** checker reports a violation for a generated 151-effective-line file in a passing unit test.
- [x] 13.048 Configure pytest to emit JUnit XML and timestamped run logs into reports/ | **Status:** DONE | **DoD:** after `uv run pytest`, reports/junit.xml and a timestamped log file both exist.
- [x] 13.049 Document the expected result per test via a mandatory "Expected:" docstring line enforced by a meta-test | **Status:** DONE | **DoD:** meta-test fails for any test function lacking an "Expected:" docstring line and currently passes on the suite.
- [x] 13.050 Implement the automated pass/fail report generator producing reports/test_report.md from the JUnit XML | **Status:** DONE | **DoD:** generated report lists every test with name, expected result, and PASS/FAIL status.
- [x] 13.051 Configure per-module coverage targets check reading coverage JSON (gatekeeper >=90, sdk >=95, agents >=85, graphops >=85) | **Status:** DONE | **DoD:** check command exits non-zero when any module is below its target in coverage.json.
- [x] 13.052 Run mutation-style spot checks on the analysis engine (invert the 0.91 threshold, swap hub/bottleneck branch, drop a centrality normalization) | **Status:** DONE | **DoD:** each of the three hand-applied mutants is killed by at least one failing test, recorded in reports/mutation_spotchecks.md.
- [x] 13.053 Write a performance smoke test running the fully mocked end-to-end pipeline on fixture_repo under a wall-clock budget | **Status:** DONE | **DoD:** smoke test asserts the mocked pipeline completes in under 30 seconds and passes.
- [x] 13.054 Verify the full local quality gate: `uv run pytest` with coverage >=85 and `uv run ruff check .` with zero violations | **Status:** DONE | **DoD:** both commands exit 0 in one recorded run whose log is saved under reports/.
- [x] 13.055 Verify TDD red-green evidence across modules and mark Phase 13 complete in docs/TODO.md | **Status:** DONE | **DoD:** git history shows a failing-test commit preceding each module implementation commit and the Phase 13 gate entry in docs/TODO.md is updated to DONE.

## Phase 14 — SKILL.md, LLM Wiki & Knowledge Assets

- [x] 14.001 Analyze MATERIALS (Part B, Guidelines V3) knowledge-asset requirements and map SKILL.md, raw->wiki, and 4-metric mandates to Phase 14 task IDs | **Status:** DONE | **DoD:** Every Part B knowledge-asset requirement is covered by at least one task in this section (traceability check against MATERIALS passes).
- [x] 14.002 Review and extend the knowledge-assets FR group in docs/PRD.md covering skill files, raw->wiki pipeline, and the 4-metric rubric | **Status:** DONE | **DoD:** The docs/PRD.md knowledge-assets FR group contains at least 8 distinct FR-xx requirement IDs.
- [x] 14.003 Define knowledge-asset constants in src/archlens/shared/constants.py (SKILLS_DIR, RAW_DIR, WIKI_DIR, INDEX_FILE, LOG_FILE, RUBRIC_MIN_SCORE=0, RUBRIC_MAX_SCORE=10) | **Status:** DONE | **DoD:** `uv run python -c "from archlens.shared import constants"` exits 0 with all seven names defined.
- [x] 14.004 Configure the knowledge_assets block in config/setup.json (vault raw/ and wiki/ paths, skills directory, eval task-set path, metrics output path) with assertions in tests/test_setup_config.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_setup_config.py` passes with all five keys asserted present and non-empty.
- [x] 14.005 Write failing tests in tests/test_skill_schema.py for SKILL.md validation (YAML frontmatter requires name, description, allowed-tools; missing or empty fields rejected) | **Status:** DONE | **DoD:** Red run captured before implementation (suite fails with assertion/import errors) per TDD.
- [x] 14.006 Implement the skill schema validator in src/archlens/vault/skill_schema.py exposed only via sdk.validate_skill | **Status:** DONE | **DoD:** `uv run pytest tests/test_skill_schema.py` passes and the file is <= 150 code lines.
- [x] 14.007 Write failing tests in tests/test_skill_guardrails.py for guardrail classification (read-only -> auto, reversible -> undo path required, irreversible -> explicit human approval, disable-model-invocation -> never auto-invocable) | **Status:** DONE | **DoD:** Red run captured before implementation per TDD with all four guardrail levels asserted.
- [x] 14.008 Implement the guardrail classifier in src/archlens/vault/skill_guardrails.py mapping declared tools and step markers to guardrail levels | **Status:** DONE | **DoD:** `uv run pytest tests/test_skill_guardrails.py` passes covering all four levels.
- [x] 14.009 Write SKILL_graph_reading.md YAML frontmatter with name, routing description, and allowed tools restricted to read-only operations (Read, Grep, Glob, graph queries) | **Status:** DONE | **DoD:** sdk.validate_skill returns zero errors for SKILL_graph_reading.md.
- [x] 14.010 Write the "# When to use" section of SKILL_graph_reading.md listing sharp trigger phrases and problem patterns (e.g., "which module imports X", "rank hot nodes", hot.md triage) | **Status:** DONE | **DoD:** Section contains >= 5 distinct trigger phrases and >= 3 problem patterns.
- [x] 14.011 Write the "# Procedure" section of SKILL_graph_reading.md enforcing the relation -> confidence -> source_file citation discipline for every claim, with one worked example citing a real graph.json edge | **Status:** DONE | **DoD:** Procedure shows the citation triple in that exact order and includes one concrete graph.json edge example.
- [x] 14.012 Verify SKILL_graph_reading.md guardrail level is "auto" (read-only) with no write or execute tools in allowed-tools | **Status:** DONE | **DoD:** A dedicated assertion in tests/test_skill_guardrails.py passes against the real skill file.
- [x] 14.013 Write SKILL_refactor.md YAML frontmatter with name, routing description, allowed tools, and disable-model-invocation set on human-only entry points | **Status:** DONE | **DoD:** sdk.validate_skill returns zero errors and the disable-model-invocation flag is present in frontmatter.
- [x] 14.014 Write the "# When to use" section of SKILL_refactor.md with trigger phrases tied to bug classes (split god module, break bottleneck, merge duplicates with similarity >= 0.91, fix SPOF) | **Status:** DONE | **DoD:** Section lists >= 5 trigger phrases, each mapped to a BugHunterAgent bug class.
- [x] 14.015 Write the reversible steps of the SKILL_refactor.md "# Procedure" so every step documents a concrete git-based undo path (branch name plus revert command) | **Status:** DONE | **DoD:** Guardrail validator reports 0 reversible steps missing an explicit undo command.
- [x] 14.016 Document irreversible actions in SKILL_refactor.md (file deletion, merge finalization) with explicit "APPROVAL REQUIRED" markers before execution | **Status:** DONE | **DoD:** Guardrail validator reports 0 unlabeled irreversible steps in the file.
- [x] 14.017 Verify SKILL_refactor.md end-to-end against schema and guardrail validators (frontmatter valid, undo paths present, approvals marked, human-only steps not auto-invocable) | **Status:** DONE | **DoD:** `uv run pytest tests/test_skill_guardrails.py -k refactor` passes.
- [x] 14.018 Write failing tests in tests/test_skill_router.py for skill routing (exact trigger phrase -> correct skill, ambiguous phrase -> no match, disable-model-invocation skills never auto-returned) | **Status:** DONE | **DoD:** Red run captured before implementation per TDD.
- [x] 14.019 Implement the skill router in src/archlens/vault/skill_router.py matching prompts against skill description trigger phrases and problem patterns | **Status:** DONE | **DoD:** `uv run pytest tests/test_skill_router.py` passes and the file is <= 150 code lines.
- [x] 14.020 Verify routing precision on a 20-prompt fixture set covering both skills plus 5 distractor prompts | **Status:** DONE | **DoD:** >= 18/20 prompts routed correctly with 0 auto-invocations of disable-model-invocation skills.
- [x] 14.021 Write failing tests in tests/test_raw_ingest.py for raw/ ingestion (graph.json, REPORT.md, hot.md copied into vault raw/ with provenance headers containing source path and timestamp) | **Status:** DONE | **DoD:** Red run captured before implementation per TDD.
- [x] 14.022 Implement raw/ ingestion in src/archlens/vault/raw_ingest.py via the SDK | **Status:** DONE | **DoD:** `uv run pytest tests/test_raw_ingest.py` passes and every ingested file carries a provenance header.
- [x] 14.023 Write failing tests in tests/test_wiki_builder.py for the raw/ -> wiki/ build (one distilled page per topic, each backlinking its raw/ source) | **Status:** DONE | **DoD:** Red run captured before implementation per TDD.
- [x] 14.024 Implement the wiki builder in src/archlens/vault/wiki_builder.py producing focused wiki/ pages with raw/ backlinks | **Status:** DONE | **DoD:** `uv run pytest tests/test_wiki_builder.py` passes and every generated page contains >= 1 raw/ backlink.
- [x] 14.025 Write failing tests in tests/test_index_hub.py for the index.md hub generator (read-first hub linking 2-3 wiki pages per topic with one-line descriptions) | **Status:** DONE | **DoD:** Red run captured before implementation per TDD.
- [x] 14.026 Implement the index.md hub generator in src/archlens/vault/index_builder.py | **Status:** DONE | **DoD:** `uv run pytest tests/test_index_hub.py` passes and each topic entry links 2-3 wiki pages.
- [x] 14.027 Write failing tests in tests/test_wiki_log.py for log.md journaling (append-only entries with timestamp, source, action, outcome; insertion order preserved) | **Status:** DONE | **DoD:** Red run captured before implementation per TDD.
- [x] 14.028 Implement the log.md ingestion journal writer in src/archlens/vault/wiki_log.py and invoke it from every raw/wiki build step | **Status:** DONE | **DoD:** `uv run pytest tests/test_wiki_log.py` passes and a full vault build writes >= 1 entry per pipeline stage.
- [x] 14.029 Test wiki integrity end-to-end: every wiki/ page reachable from index.md within 2 hops and zero orphan pages | **Status:** DONE | **DoD:** tests/test_wiki_integrity.py passes against the generated vault.
- [x] 14.030 Write failing tests in tests/test_knowledge_rubric.py for the rubric model (exactly 4 metrics, integer 0-10 scales, mandatory rationale string, out-of-range scores rejected) | **Status:** DONE | **DoD:** Red run captured before implementation per TDD.
- [x] 14.031 Implement the 4-metric rubric model in src/archlens/metrics/knowledge_rubric.py (source traceability, noise reduction, correct-file identification, correct-tool timing) | **Status:** DONE | **DoD:** `uv run pytest tests/test_knowledge_rubric.py` passes and the file is <= 150 code lines.
- [x] 14.032 Implement the source-traceability scorer mapping the ratio of claims citing relation -> confidence -> source_file to a 0-10 score | **Status:** DONE | **DoD:** Boundary unit tests pass (0% cited -> score 0, 100% cited -> score 10).
- [x] 14.033 Implement the noise-reduction scorer comparing irrelevant context tokens loaded before vs after wiki use | **Status:** DONE | **DoD:** Boundary unit tests pass (no reduction -> score 0, full reduction -> score 10).
- [x] 14.034 Implement the correct-file identification scorer using precision/recall of files opened vs ground-truth relevant files | **Status:** DONE | **DoD:** Boundary unit tests pass (perfect match -> 10, zero overlap -> 0).
- [x] 14.035 Implement the correct-tool timing scorer comparing the invoked skill/tool sequence against the expected sequence per task | **Status:** DONE | **DoD:** Boundary unit tests pass (exact sequence -> 10, fully wrong order -> 0).
- [x] 14.036 Verify coverage for rubric and scorer modules (statement + branch) | **Status:** DONE | **DoD:** `uv run pytest --cov=src/archlens/metrics` reports >= 85% for knowledge_rubric.py and all four scorers.
- [x] 14.037 Write the fixed evaluation task set (10 repository questions, each with ground-truth files, expected claims, and expected tool sequence) as a versioned fixture | **Status:** DONE | **DoD:** Fixture file exists with exactly 10 tasks, each containing all three ground-truth fields.
- [x] 14.038 Write failing tests in tests/test_knowledge_eval.py for the measurement harness (runs baseline or assisted mode, records 4 scores plus token counts per task, emits schema-valid JSON) | **Status:** DONE | **DoD:** Red run captured before implementation per TDD.
- [x] 14.039 Implement the measurement harness in src/archlens/metrics/knowledge_eval.py supporting baseline (no wiki/skills) and assisted (wiki + skills) modes via the SDK | **Status:** DONE | **DoD:** `uv run pytest tests/test_knowledge_eval.py` passes and the file is <= 150 code lines.
- [x] 14.040 Execute the baseline (before) measurement run on the 10-task evaluation set and persist results | **Status:** DONE | **DoD:** metrics/knowledge_baseline.json exists with 4 scores and token counts for all 10 tasks.
- [x] 14.041 Execute the assisted (after) measurement run and generate the before/after comparison table | **Status:** DONE | **DoD:** metrics/knowledge_assisted.json exists and the comparison table shows a delta for each of the 4 metrics.
- [x] 14.042 Implement the iterate cycle in src/archlens/metrics/knowledge_iterate.py (score -> select weakest metric -> apply targeted wiki/skill correction -> re-measure, journaled to log.md, stopping when all metrics meet target or after 3 cycles) | **Status:** DONE | **DoD:** Cycle unit tests pass including both stop conditions.
- [x] 14.043 Execute at least one correction iteration on the live vault and verify the outcome | **Status:** DONE | **DoD:** Re-measured weakest metric strictly improves (or a written rationale is journaled) and log.md contains the full cycle entry.
- [x] 14.044 Verify Ruff compliance and the 150-line limit across all Phase 14 modules and tests | **Status:** DONE | **DoD:** `uv run ruff check src/archlens/vault src/archlens/metrics tests` exits 0 and the line-count check reports 0 files over 150 code lines.
- [x] 14.045 Verify the full knowledge-asset validation suite and the global coverage gate after Phase 14 integration | **Status:** DONE | **DoD:** `uv run pytest --cov` passes with fail_under=85 and zero failing knowledge-asset tests.

## Phase 15 — Research, Visualization & Sensitivity Analysis

- [x] 15.001 Add notebook tooling dependencies (jupyter, nbconvert, matplotlib, seaborn, pandas, numpy) to the dev group in pyproject.toml via `uv add --dev` | **Status:** DONE | **DoD:** `uv run jupyter --version` exits 0 and uv.lock contains the new dependency entries.
- [x] 15.002 Configure a `sensitivity` block in config/setup.json defining OAT ranges for analysis_depth, top_k_pages, rate_limit_rpm, similarity_threshold plus baseline values and run_count >= 3 | **Status:** DONE | **DoD:** config loader test passes and the block contains all four parameter keys with list-valued ranges.
- [x] 15.003 Write failing unit tests in tests/test_sensitivity_grid.py for OAT variant generation (one factor varied, all others held at baseline) | **Status:** DONE | **DoD:** `uv run pytest tests/test_sensitivity_grid.py` fails red because the module does not exist yet.
- [x] 15.004 Implement src/archlens/metrics/sensitivity_grid.py generating OAT variants exclusively from cfg.get values and exposed only via the SDK | **Status:** DONE | **DoD:** `uv run pytest tests/test_sensitivity_grid.py` passes and the file is <= 150 code lines.
- [x] 15.005 Write failing unit tests in tests/test_sensitivity_runner.py covering per-variant pipeline execution stubs and JSON result persistence paths | **Status:** DONE | **DoD:** `uv run pytest tests/test_sensitivity_runner.py` fails red before implementation.
- [x] 15.006 Implement src/archlens/metrics/sensitivity_runner.py executing one pipeline run per OAT variant through sdk.py and writing results/sensitivity/<param>.json | **Status:** DONE | **DoD:** `uv run pytest tests/test_sensitivity_runner.py` passes with mocked SDK calls and zero direct API calls outside the gatekeeper.
- [x] 15.007 Write failing unit tests in tests/test_variance_stats.py for mean, standard deviation and coefficient-of-variation computed across repeated run records | **Status:** DONE | **DoD:** `uv run pytest tests/test_variance_stats.py` fails red before implementation.
- [x] 15.008 Implement src/archlens/metrics/variance_stats.py computing per-metric mean/std/CV across N runs and exporting a CSV summary | **Status:** DONE | **DoD:** `uv run pytest tests/test_variance_stats.py` passes and the file is <= 150 code lines.
- [x] 15.009 Write failing unit tests in tests/test_chart_factory.py asserting each chart function (bar, line, scatter, heatmap, box, waterfall) writes a non-empty PNG to a given path | **Status:** DONE | **DoD:** `uv run pytest tests/test_chart_factory.py` fails red with six parametrized cases.
- [x] 15.010 Implement src/archlens/metrics/chart_factory.py with bar, line, scatter, heatmap, box and waterfall renderers using matplotlib with the Agg backend | **Status:** DONE | **DoD:** all six tests in tests/test_chart_factory.py pass and each produced PNG file is > 0 bytes.
- [x] 15.011 Write failing unit tests in tests/test_iso25010_mapping.py asserting the mapping emits a Markdown table covering at least 6 ISO/IEC 25010 characteristics | **Status:** DONE | **DoD:** `uv run pytest tests/test_iso25010_mapping.py` fails red before implementation.
- [x] 15.012 Implement src/archlens/metrics/iso25010_mapping.py mapping measured ArchLens metrics (coverage, modularity, token savings, ruff count) to ISO/IEC 25010 characteristics | **Status:** DONE | **DoD:** `uv run pytest tests/test_iso25010_mapping.py` passes and the emitted table has >= 6 characteristic rows.
- [x] 15.013 Execute the full pipeline 3 times at the baseline configuration via `uv run` and persist metrics snapshots to results/variance/run_1.json through run_3.json | **Status:** DONE | **DoD:** three JSON files exist, each containing token, modularity and runtime fields.
- [x] 15.014 Execute the OAT sweep over analysis depth values from config/setup.json and persist outcomes | **Status:** DONE | **DoD:** results/sensitivity/analysis_depth.json exists with one record per configured depth value.
- [x] 15.015 Execute the OAT sweep over top-k retrieval pages (k values from config) measuring answer-quality proxy and token cost per k | **Status:** DONE | **DoD:** results/sensitivity/top_k_pages.json exists with one record per configured k value.
- [x] 15.016 Execute the OAT sweep over rate-limit settings (req/min variants from config/rate_limits.json) recording total throttle wait time and queue depth per variant | **Status:** DONE | **DoD:** results/sensitivity/rate_limits.json exists with wait-time and max-queue-depth fields per variant.
- [x] 15.017 Execute the OAT sweep over duplicate-similarity threshold values bracketing 0.91 and record validated-duplicate counts per threshold | **Status:** DONE | **DoD:** results/sensitivity/similarity_threshold.json exists with a duplicate count for every configured threshold.
- [x] 15.018 Compute variance statistics across the 3 baseline runs via the variance_stats module and export results/variance/summary.csv | **Status:** DONE | **DoD:** summary.csv exists with mean, std and CV columns populated for every tracked metric.
- [x] 15.019 Generate the token-usage bar chart (baseline naive run vs Graphify-assisted run, grouped per agent) and save it to docs/assets/tokens_bar.png | **Status:** DONE | **DoD:** docs/assets/tokens_bar.png exists and renders both run series for all agents.
- [x] 15.020 Generate the waterfall chart decomposing total token savings by pipeline stage (detect, extract, build, cluster, export, analysis) into docs/assets/tokens_waterfall.png | **Status:** DONE | **DoD:** docs/assets/tokens_waterfall.png exists and the final bar equals the reported total savings figure.
- [x] 15.021 Generate the stop-condition convergence line plot (inter-community edges, bottleneck dependency count, tests-green flag per improvement-loop iteration) into docs/assets/convergence_line.png | **Status:** DONE | **DoD:** docs/assets/convergence_line.png exists, plots <= 5 iterations and visually marks the stopping iteration.
- [x] 15.022 Generate the scatter plot of similarity threshold vs validated-duplicate count with the 0.91 triage threshold annotated, saved to docs/assets/similarity_scatter.png | **Status:** DONE | **DoD:** docs/assets/similarity_scatter.png exists with a vertical annotation at x = 0.91.
- [x] 15.023 Generate the sensitivity heatmap of OAT parameter vs normalized outcome delta across all four swept parameters, saved to docs/assets/sensitivity_heatmap.png | **Status:** DONE | **DoD:** docs/assets/sensitivity_heatmap.png exists with one row per swept parameter and a colorbar legend.
- [x] 15.024 Generate the box plot of run-to-run variance for total tokens and runtime across the 3 baseline runs, saved to docs/assets/variance_box.png | **Status:** DONE | **DoD:** docs/assets/variance_box.png exists showing one box per tracked metric.
- [x] 15.025 Generate the grouped before/after bar chart for the 4 knowledge metrics (source traceability, noise reduction, correct-file identification, correct-tool-at-the-right-time) into docs/assets/four_metrics_bar.png | **Status:** DONE | **DoD:** docs/assets/four_metrics_bar.png exists with before and after bars for all four metrics.
- [x] 15.026 Create notebooks/archlens_analysis.ipynb skeleton with the house-style header cell (Version 1.00, course line), table of contents and empty section placeholders | **Status:** DONE | **DoD:** the notebook file exists and `uv run jupyter nbconvert --to html` on it exits 0.
- [x] 15.027 Write the notebook introduction section stating the research questions with LaTeX-formatted definitions of token savings and modularity deltas | **Status:** DONE | **DoD:** nbconvert HTML output renders the LaTeX equations in the introduction without raw `$` artifacts.
- [x] 15.028 Write the notebook methodology section describing the OAT sensitivity design with LaTeX notation for parameters, baselines and one-factor deltas | **Status:** DONE | **DoD:** the methodology cell names all four swept parameters and renders at least two LaTeX equations.
- [x] 15.029 Write the academic references cell citing Liu et al. 2024 (Lost in the Middle) and Vaswani et al. 2017 (Attention Is All You Need) plus at least two further sources as a numbered bibliography | **Status:** DONE | **DoD:** the references cell contains both required citations with author, year and venue, totaling >= 4 entries.
- [x] 15.030 Write the notebook discussion subsection linking Liu et al. 2024 context-position findings to ArchLens graph-scoped retrieval and hot.md-first reading order | **Status:** DONE | **DoD:** the subsection cites Liu et al. 2024 inline and cross-references the token-savings chart by figure number.
- [x] 15.031 Embed all generated charts into the notebook with numbered captions, covering at least 4 distinct chart types | **Status:** DONE | **DoD:** nbconvert HTML output displays every docs/assets PNG from this phase with a caption, spanning >= 4 chart types.
- [x] 15.032 Write the token-economics results section with the per-model cost table (input/output tokens, $ cost) and the computed savings percentage vs the naive baseline | **Status:** DONE | **DoD:** the section reports savings >= 70% or includes the mandated written amortization explanation per Part A.
- [x] 15.033 Write the 4-metrics before/after analysis section quantifying the delta for each knowledge metric against the grouped bar chart | **Status:** DONE | **DoD:** each of the four metrics has a numeric before value, after value and delta stated in the text.
- [x] 15.034 Write the stop-condition convergence analysis interpreting the line plot against all Part C diff-metric stop conditions | **Status:** DONE | **DoD:** every stop condition (bottleneck dependency loss, modularity, no new isolated components, tests green, ruff 0) is mapped to a plotted series or an explicit measured value.
- [x] 15.035 Insert the ISO/IEC 25010 quality mapping table generated by iso25010_mapping into the notebook results section | **Status:** DONE | **DoD:** the rendered table shows >= 6 ISO/IEC 25010 characteristics each mapped to one measured ArchLens metric.
- [x] 15.036 Write the findings and threats-to-validity section summarizing sensitivity rankings, variance levels and study limitations | **Status:** DONE | **DoD:** the section lists >= 3 findings, each citing a specific figure or table by number.
- [x] 15.037 Verify the notebook executes cleanly top-to-bottom via `uv run jupyter nbconvert --to notebook --execute notebooks/archlens_analysis.ipynb` | **Status:** DONE | **DoD:** the command exits 0 with zero cell execution errors on a fresh kernel.
- [x] 15.038 Verify lint and line-limit compliance for all Phase 15 modules and tests | **Status:** DONE | **DoD:** `uv run ruff check .` reports 0 violations and every new file is <= 150 code lines (blank/comment lines excluded).
- [x] 15.039 Verify project coverage including the Phase 15 metrics modules via `uv run pytest --cov` | **Status:** DONE | **DoD:** the coverage gate passes with fail_under=85 and no Phase 15 module below 85% statement+branch coverage.
- [ ] 15.040 Obtain lecturer sign-off on the research notebook, sensitivity results and findings writeup | **Status:** BLOCKED | **DoD:** written lecturer approval is recorded in docs/TODO.md next to this task with date and approver name.

## Phase 16 — CI/CD, README, Prompt Book & Final Submission

- [x] 16.001 Write failing pytest tests in tests/test_check_line_cap.py covering the 150-line cap checker (blank and comment lines excluded, tests directory included in scope) | **Status:** DONE | **DoD:** `uv run pytest tests/test_check_line_cap.py` exits non-zero (red) against a stub checker
- [x] 16.002 Implement scripts/check_line_cap.py counting only non-blank, non-comment lines per code file and exiting non-zero on any file over 150 | **Status:** DONE | **DoD:** `uv run pytest tests/test_check_line_cap.py` exits 0 and the script exits 0 on the full repo
- [x] 16.003 Write failing pytest tests in tests/test_check_forbidden_tools.py for a scanner that flags pip, virtualenv, venv and "python -m" mentions in code, docs and CI files | **Status:** DONE | **DoD:** `uv run pytest tests/test_check_forbidden_tools.py` exits non-zero (red) before implementation
- [x] 16.004 Implement scripts/check_forbidden_tools.py with an allowlist for guideline-quotation contexts | **Status:** DONE | **DoD:** `uv run pytest tests/test_check_forbidden_tools.py` exits 0 and `uv run python scripts/check_forbidden_tools.py` reports 0 hits repo-wide
- [x] 16.005 Create .github/workflows/ci.yml installing uv via astral-sh/setup-uv and running `uv sync --frozen` as the only dependency-install step | **Status:** DONE | **DoD:** actionlint reports 0 errors and the workflow file contains zero pip, venv or "python -m" commands
- [x] 16.006 Add Ruff gate step `uv run ruff check .` to ci.yml failing the build on any violation | **Status:** DONE | **DoD:** a branch seeded with one deliberate violation turns the CI check red and returns green after the violation is removed
- [x] 16.007 Add coverage gate step `uv run pytest --cov=src/archlens --cov-branch` to ci.yml enforcing fail_under=85 from pyproject.toml | **Status:** DONE | **DoD:** CI log shows total coverage >= 85% and the step exit code is 0 on main
- [x] 16.008 Add line-cap and forbidden-tooling gate steps to ci.yml running both scripts via `uv run python` | **Status:** DONE | **DoD:** both steps appear in the CI run log and exit 0 on the current main branch
- [x] 16.009 Add a gitleaks secrets-scan job to ci.yml scanning the full git history | **Status:** DONE | **DoD:** the gitleaks job completes with 0 findings on the PR check run
- [x] 16.010 Configure ci.yml triggers for push to main and pull_request with uv dependency caching and concurrency cancellation of superseded runs | **Status:** DONE | **DoD:** two rapid pushes to one PR show the first run auto-cancelled and the second run green with a cache hit logged
- [ ] 16.011 Test the full CI pipeline end-to-end by opening a draft PR from a feature branch | **Status:** TODO | **DoD:** all CI gates (sync, ruff, coverage, line-cap, forbidden-tooling, gitleaks) show green on the PR checks tab
- [x] 16.012 Write CONTRIBUTING.md documenting feature-branch naming, PR review rules, commit message style and the uv-only command policy | **Status:** DONE | **DoD:** CONTRIBUTING.md exists with all four sections and passes scripts/check_forbidden_tools.py
- [x] 16.013 Create .github/PULL_REQUEST_TEMPLATE.md with a gate checklist (tests green, coverage >= 85, Ruff 0, 150-line cap, no secrets, docs updated) | **Status:** DONE | **DoD:** a newly opened PR auto-populates the template with all six checklist items
- [ ] 16.014 Configure branch protection on main requiring the CI status checks and one PR review before merge | **Status:** TODO | **DoD:** `gh api repos/:owner/:repo/branches/main/protection` returns required_status_checks and a direct push to main is rejected
- [x] 16.015 Write README.md skeleton with the mandated header (Version 1.00, Status, Course) and a table of contents for all manual sections | **Status:** DONE | **DoD:** README.md exists with the exact header line and a ToC entry for every planned section
- [x] 16.016 Document uv-only installation in README (clone, `uv sync`, copy .env-example to .env) | **Status:** DONE | **DoD:** following the steps verbatim on a clean clone produces a working environment with zero deviations
- [x] 16.017 Document CLI usage examples in README using real `uv run python src/main.py` invocations with captured output blocks | **Status:** DONE | **DoD:** every documented command re-runs with exit code 0 and matches the captured output shape
- [x] 16.018 Capture screenshots of a full CLI run, the graph.html visualization and the Obsidian vault into docs/screenshots/ | **Status:** DONE | **DoD:** docs/screenshots/ contains at least 3 PNG files covering cli_run, graph_html and obsidian_vault
- [x] 16.019 Embed the screenshots in README with captions and alt text | **Status:** DONE | **DoD:** all embedded images render in GitHub preview with non-empty alt text
- [x] 16.020 Write README config reference tables covering every key in config/setup.json, config/rate_limits.json and config/logging_config.json (name, type, default, effect) | **Status:** DONE | **DoD:** 100% of keys present in the three JSON files appear in the reference tables
- [x] 16.021 Add a README architecture overview with a fenced mermaid block diagram of the agent graph and links to docs/PRD.md and docs/PLAN.md | **Status:** DONE | **DoD:** the mermaid block renders on GitHub and both document links resolve
- [x] 16.022 Add the token-cost summary table to README (baseline naive run vs Graphify-assisted run, input/output tokens and $ cost per model) | **Status:** DONE | **DoD:** table shows >= 70% savings, or the written graph-scan amortization explanation appears directly beneath it
- [x] 16.023 Add a README contribution section linking CONTRIBUTING.md and the PR template | **Status:** DONE | **DoD:** the section exists and both relative links resolve in GitHub preview
- [x] 16.024 Add a README license and credits section (course L07 materials, Graphify, BugsInPy, third-party dependencies) | **Status:** DONE | **DoD:** the section lists a license identifier plus all four credit categories
- [x] 16.025 Verify README end-to-end by running a link checker and copy-paste-executing every documented command | **Status:** DONE | **DoD:** 0 broken links and every documented command exits 0
- [x] 16.026 Conduct a Nielsen 10-heuristics evaluation of the CLI and generated outputs, recording each finding with severity 0-4 | **Status:** DONE | **DoD:** docs/UX_HEURISTICS.md covers all 10 heuristics, each with a verdict and an evidence screenshot reference
- [x] 16.027 Write failing pytest tests for the planned CLI usability fixes (help text completeness, actionable error messages, progress/status lines) | **Status:** DONE | **DoD:** the new tests exit non-zero (red) against the current CLI
- [x] 16.028 Implement CLI usability fixes for every severity >= 3 heuristic finding | **Status:** DONE | **DoD:** tests from 16.027 pass, `uv run ruff check .` reports 0 violations and no touched file exceeds the 150-line cap
- [x] 16.029 Capture after-fix screenshots and append a before/after comparison to docs/UX_HEURISTICS.md | **Status:** DONE | **DoD:** every severity >= 3 finding shows a before and an after screenshot marked resolved
- [x] 16.030 Log all doc-authoring prompt sessions (PRD, PLAN, specialized PRDs, TODO) into docs/PROMPT_BOOK.md with prompt text, model and outcome | **Status:** DONE | **DoD:** each of the doc types has at least one complete PROMPT_BOOK entry with all three fields
- [x] 16.031 Sweep every agent system prompt (RepoAgent, GraphAgent, AnalystAgent, BugHunterAgent, RefactorAgent, QAAgent, MetricsAgent) into PROMPT_BOOK.md with model parameters and iteration notes | **Status:** DONE | **DoD:** all 7 agents have an entry containing prompt text, parameters and at least one iteration note
- [x] 16.032 Verify PROMPT_BOOK completeness by cross-referencing every LLM call site in src/archlens/gatekeeper/gatekeeper.py against entry IDs | **Status:** DONE | **DoD:** 100% of gatekeeper LLM call sites map to a cited PROMPT_BOOK entry ID
- [x] 16.033 Execute the Guidelines V3 Table-5 quick-reference compliance sweep item by item, recording evidence per row | **Status:** DONE | **DoD:** docs/COMPLIANCE_TABLE5.md marks every Table-5 row PASS with an evidence link
- [x] 16.034 Verify the coverage gate locally with `uv run pytest --cov=src/archlens --cov-branch` | **Status:** DONE | **DoD:** the command exits 0 with reported total coverage >= 85%
- [x] 16.035 Verify zero lint violations repo-wide with `uv run ruff check .` | **Status:** DONE | **DoD:** the command exits 0 reporting all checks passed
- [x] 16.036 Verify the 150-line cap on every code file including tests with `uv run python scripts/check_line_cap.py` | **Status:** DONE | **DoD:** the script exits 0 listing zero offending files
- [x] 16.037 Audit the codebase for hardcoded values outside shared/constants.py and Enums (literal URLs, paths, magic numbers, model names) | **Status:** DONE | **DoD:** the audit yields 0 violations, with any hit refactored to cfg.get or os.environ.get before sign-off
- [x] 16.038 Verify dependency hygiene: pyproject.toml as single source of truth, committed uv.lock and zero requirements.txt files | **Status:** DONE | **DoD:** `git ls-files` shows uv.lock tracked and no file matching requirements*.txt
- [x] 16.039 Run a local gitleaks secrets scan and verify .env is git-ignored while .env-example with dummy values is tracked | **Status:** DONE | **DoD:** gitleaks reports 0 findings, `git check-ignore .env` exits 0 and .env-example appears in `git ls-files`
- [x] 16.040 Verify the deliverables inventory: graph.json, graph.html, REPORT.md, vault (hot.md, index.md, wiki/, log.md), block diagram, OOP class schema, PRD-vs-code audit, SKILL.md and all docs/ files | **Status:** DONE | **DoD:** every listed artifact exists at its documented repository path
- [x] 16.041 Write docs/SUBMISSION_CHECKLIST.md enumerating every EX04 deliverable and quality gate, then mark each item complete | **Status:** DONE | **DoD:** the file exists with zero unchecked items remaining
- [ ] 16.042 Tag the release by creating annotated git tag v1.00 on the CI-green main commit and pushing it to origin | **Status:** TODO | **DoD:** `git ls-remote --tags origin` lists refs/tags/v1.00 pointing at a commit with green CI
- [ ] 16.043 Package and submit the final deliverables (repo link at tag v1.00 plus required attachments) per the course submission instructions | **Status:** TODO | **DoD:** a submission confirmation exists with a timestamp before the deadline
- [ ] 16.044 Obtain lecturer confirmation that the submission was received and is reviewable | **Status:** BLOCKED | **DoD:** written lecturer acknowledgement is recorded in docs/SUBMISSION_CHECKLIST.md
- [x] 16.045 Write post-submission retrospective notes covering process, token-savings outcome and lessons learned | **Status:** DONE | **DoD:** docs/RETRO.md exists with sections for process, metrics outcome and at least 3 lessons learned
